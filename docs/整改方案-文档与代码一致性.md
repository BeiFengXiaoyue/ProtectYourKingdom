# 文档与代码一致性审计 · 整改方案

> 提出：集成/架构视角审计 ｜ 日期：2026-09-10 ｜ 适用版本：dev（feature 分支）
> 状态：**待评审**（本文档只固化"问题 + 整改动作"，**本轮不改任何既有文档、不改任何 `.java`、不改 `resources` 配置**）
> 审计对象：`docs/` 全部 8 份文档与契约 ↔ `src/main/java` 全量代码 ↔ `src/main/resources`（`config/` `assets/` `maps/`）
> 关联文档：《接口契约与抽象类说明》《数值配置JSON化规范》《关卡波次配置规范》《视觉尺寸配置规范》《防御塔子系统说明》《友方士兵子系统说明》《建筑与怪物机制策划 v1.1》《单位行为动画-渲染层设计》《音效扩展方法说明》、`README.md`

---

## 0. 本文档性质与红线

| 项 | 说明 |
| :--- | :--- |
| 性质 | **整改工单**。第 6 节是"文档怎么改"，第 7 节是"代码怎么改"，第 8 节是"数据/资产怎么改"；每项含目标、位置、步骤、验收 |
| 红线 1 | 本文档只定义方案，**不执行**。执行顺序见 §9 |
| 红线 2 | 公共抽象类与 `GameController`/`GameConfig` **仅 A 可改**（既有惯例），执行方交付 diff 由 A review 合入 |
| 红线 3 | 不得新增第二套数值/尺寸/波次通道；本章所有"目标位置"列的即为**唯一归口** |

---

## 1. 审计范围与方法

- **范围**：`docs/` 8 份 + `README.md`；`src/main/java/com/kingdom/game/**`（controller / model / view / config / util 全量子包）；`src/main/resources/{config,assets,maps}`。
- **方法**：逐条契约声明 → 定位实现 → 判定"一致 / 文档过期 / 代码缺口 / 双方冲突"，每条结论附 `文件:行号`。
- **结论分级**：
  - **P0**：会让"配置/契约"失效或误导的高危项（数值假通道、数值零通道）；
  - **P1**：文档已声明"已实现/已确认"但代码缺失，或运行期功能断链；
  - **P2**：文档过期、自相矛盾、口径不统一。

---

## 2. 结论摘要

| 分级 | 数量 | 一句话 |
| :--- | :--- | :--- |
| P0 | 5 | 敌人攻击力等战斗数值**零配置通道**；`balance.json` 存在**假配置通道**（写了不生效、不告警）；`totalWaves` 静默失效；塔/士兵数值同样零通道；配置规范文档自身与实现不符且状态过期 |
| P1 | 13 | 重甲减伤/破甲、Boss 震地/召唤/狂暴攻击间隔、溅射半径传递、三级链 Master 断链、选中通知接线、Boss 预警、兵营产高级兵 —— 均为"文档已确认/已声明，代码没有"；另有 `Tower` 死代码与 `level` 恒 1；**新发现运行期缺陷 2 处（P1-8 士兵不进入待命 idle；P1-9 兵营升级·出售不清理产出士兵）** |
| P2 | 13 | 《建筑与怪物机制策划》等多份文档状态过期、两份文档互斥（士兵 HP 50 vs 70、士兵是否分级）、尺寸种子表整体过时、动画文档待办未勾 |

> **口径修正（2026-09-10 裁定）**：§8-1（`maps/default/waves.json` 的波数）**不属于缺陷**——
> 波次表是**测试用例**数据，波数与组合由测试（F）后续按游戏性需要决定，详见 §5-6、§8-1、§9。

---

## 3. 数值归属总表（**唯一事实源**，终结多文档各说各话）

> 用法：任何"这个数值该写在哪"的争议，以本表为准。**"现在可配"列判定的口径 = 不改 Java、重编译，仅改文件即可生效。**

| 数值 | 现状位置 | 现在可配？ | 目标位置（唯一归口） | 生效时机 |
| :--- | :--- | :--- | :--- | :--- |
| 开局金币 / 生命 | `config/balance.json` → `BalanceTable` → `GameConfig` | ✅ | 同现状 | 启动读取一次 |
| 总波数 | 名义在 `balance.json`，实际被 `maps/<key>/waves.json` 覆盖（`GameConfig.java:153-159`） | ⚠️ 假字段 | **`maps/<key>/waves.json` 唯一决定**；`balance.json.totalWaves` 降级为"波次表缺失时的兜底"并加注明 | 启动读取一次 |
| 四类敌人 HP / 速度 / 赏金 | `config/balance.json`（15 键之一） | ✅ | 同现状 | 启动读取一次 |
| 四类敌人 **近战攻击力 / 攻击冷却** | ✅ **已改**：`balance.json` 8 键（默认 5/1000、4/700、9/1400、22/1100）→ 敌人构造期取值；**实体类内写死值已全删**（含 `Enemy` 基类 5/1000） | ✅ | 已落地（P0-1 + B-4，2026-09-11）。原计划的 `GameConfig` → `spawnEnemy` 显式传参路径**保留待 5-1**；当前经 `BalanceTable.runtime()` 生效 | 启动读取一次 |
| 敌人 **物理减伤 / 破甲（穿甲）** | **无字段、无实现** | ❌ | `config/balance.json`（`tankPhysicalReduction` 等）+ `Enemy.damageTakenMultiplier`，在 `Arrow/Bomb.onHit` 落地 | 启动读取一次 |
| Boss 狂暴阈值 / 速度倍率 | `BossEnemy.java:21,24` 类常量（0.5 / 1.5） | ❌ | 暂留实体类；二期入 `balance.json` | — |
| 塔 造价 | 类常量 `BUILD_COST`，`Main.java:79-87` 转成 `TowerSpec.cost` | ❌ | 类常量暂时保留；二期入 `balance.json`（`TowerSpec.cost` 从 JSON 取） | — |
| 塔 伤害 / 射程 / 冷却 | 各塔类构造内赋值 | ❌ | 塔类实例字段（现状）；二期入 `balance.json` | — |
| 塔 升级费 | 类常量 `UPGRADE_COST` → `TowerSpec`（`ArrowTower:48` 等） | ❌ | **唯一来源 = `nextLevelSpec.getCost()`**（`GameController.java:462`） | — |
| 塔 溅射半径 | ✅ **已改**：`balance.json` ⑧ 组 3 键（50/65/80）→ `BalanceTable.runtime()` → 炮塔构造期 → `Bomb` 构造；`Bomb` 内**无**硬编码 | ✅ | 已落地（P1-1，2026-09-11） | 塔构造期读 JSON |
| 士兵 HP / 速度 / 攻击 / 冷却 | `Barrack.java:48-51` 实例字段（50/40/8/800）→ `Soldier` 构造 | ❌ | 暂留 `Barrack`；二期入 `balance.json` | — |
| 士兵 视野 / 散步半径 / 拴绳 | `Soldier.java:23,29,33,45` 类常量 | ❌ | 暂留；属行为手感参数，二期再议 | — |
| 实体 宽 / 高 | `assets/sizes.json` → `SizeTable` | ✅ | 同现状 | 实体构造期 |
| 敌人路径 / 塔位 / 画布 | `maps/<key>/path.json`、`spots.json` | ✅ | 同现状 | 启动读取一次 |
| 波次组成 / 密度 / 准备时长 | `maps/<key>/waves.json` | ✅ | 同现状 | 启动读取一次 |
| 波次节奏 4 字段 | `GameConfig.java:63-68` 字面量（3 / 1000 / 5000 / 10） | ❌ | 明确**不纳入 JSON**（现行注释已声明）；`waveIntermissionMs` 可被 `waves[].maxPrepMs` 覆盖 | — |
| 配色 | `GameConfig.java:75-76` 字面量 | ❌ | 不纳入（规范 §3.4 已认定属表现） | — |

---

## 4. 专项：《数值配置JSON化规范》与实现的不一致

该规范头部 `:7` 仍自称「**状态：派工中（尚未实现）**」，但实现已全部落地：`util/balance/BalanceTable.java`、`util/balance/BalanceLibrary.java`、`src/main/resources/config/balance.json`、`GameConfig.java:98-122` 接入、`util/editor/BalanceEditorTool.java`。逐条差异：

| # | 规范原文 | 实现 | 整改 |
| :--- | :--- | :--- | :--- |
| 1 | `:7` 状态「派工中（尚未实现）」 | 已实现（含二期编辑器） | 状态改为「**已实现**」，并注明确切文件 |
| 2 | §3.2 字段表列 **10 键**，含 `waveEnemyCount` / `waveSpawnIntervalMs` / `waveIntermissionMs` / `earlyStartRewardCap` | 实际 **15 键**，**剔除**上述 4 键（`BalanceTable.java:14-25,151-156`；`GameConfig.java:26-27,62-68,122`） | §3.2 改为 15 键实表；把 4 个波次节奏键移入 §3.4「明确不纳入」并写明理由（避免与波次模块并行开发冲突） |
| 3 | §3.3 首版 seed 内容（10 键） | 实际 `balance.json` 15 键，含 `fast*` / `tank*` / `boss*` | seed 示例按实际文件重写 |
| 4 | §五·3 要求删除 10 个字段的字面量默认值 | 4 个波次节奏键的字面量仍在 `GameConfig.java:63-68` | 与 §3.2 修订保持一致：改为"删除 15 个 JSON 化字段的字面量；4 个波次节奏键保留字面量" |
| 5 | §八 验收 #7「`GameConfig` 内**搜索不到** 3/1000/5000」 | 未达成（`GameConfig.java:64,67` 仍在） | 验收项改为「搜不到 100/20/10/80/60 等**已 JSON 化**字段的字面量；3/1000/5000 允许存在于波次节奏字段」 |
| 6 | §六 建议："给《接口契约》§1（`:16-17`）util 子包列表追加 `balance`" | `接口契约与抽象类说明.md:17` **至今未补** | 执行该建议（见 §6 文档清单第 3 项） |
| 7 | §九·红线「不改实体类内的战斗数值」 | 与《关卡波次配置规范》§四 `:140`（"近战攻击/冷却由 A 在 `GameConfig` 侧按 id 补齐"）**直接冲突**，是"攻击力不可配"的根因 | 二者必须二选一：**采纳本方案 §7 P0-1**（战斗数值按 id 进 `balance.json`，实体类只做注入），并同步修订两份文档 |
| 8 | §3.4「不纳入项」（画布/路径/塔位/配色/塔目录/宽高） | 与实现一致 | 保留 |

> 🆕 **2026-09-10 第二批补注**：上表第 2/3/4 行写作时的实际键数为 **15**；本轮按 §7 P0-1 / P0-3 新增 9 键后，
> `BalanceTable` / `GameConfig` 的 JSON 化字段数已变为 **24**（8 个近战 + `tankPhysicalReduction`）。
> 《数值配置JSON化规范》§3.2/§3.4/§5·3/§8-7 已同步为 24；`balance.json` 本身仍为 15 键（新增 9 键待 F-1 写入，缺失即回退内置默认）。

---

## 5. 专项：波数唯一来源口径统一（**已裁定：`waves.json` 唯一**）

裁定：`GameConfig.java:153-159` 在读到 `waves.json` 时用其波数**覆盖** `balance.json.totalWaves` —— **判定为合理，予以确认**。据此：

1. **波数唯一来源 = `maps/<key>/waves.json`**；
2. `balance.json.totalWaves` 降级为「`waves.json` 缺失/为空/解析失败时的兜底」，**不是**并行第二通道；
3. 《数值配置JSON化规范》§3.2 需给该键加注："**通常被关卡波次表覆盖，仅作兜底**"；
4. 《关卡波次配置规范》§6.2.1「`GameState.totalWaves` 改由关卡波数提供」保留，并补一句"`balance.json.totalWaves` 仅在波次表缺失时生效（`GameConfig.java:153-159`）"；
5. `BalanceEditorTool` 的「总波数」输入框需加提示文案："编辑当前关卡默认地图时，该值会被 `maps/&lt;key&gt;/waves.json` 覆盖"；
6. **波次数据口径（已裁定）**：`maps/<key>/waves.json` 是**测试用例**数据（联调用），**不是最终关卡设计**——
   当前"9 波 / 首波 `boss_enemy × 2`"属有意为之，**不算缺陷**；波数与组合后续由测试（F）按游戏性需要决定，
   **不得写死"十波"**，也不要求按《建筑与怪物机制策划》§3.3 的十波表重做。
   本轮仅需保证文档/README **不声称固定波数**（见 §8-1、§9、§10）。

---

## 6. 文档更新清单（逐份、逐条）

> **执行状态（2026-09-10）：本清单已执行完毕。**
> 除下列例外，其余条目均按"应改为"落地（对应 10 份文档的实际改动）：
>
> - **§6.1-1**（策划文档**文件名** `-v1.0`→`-v1.1`）：⬜ **未做**（重命名会使其引用失配，待决定）；
> - **§6.3-8**（《接口契约》§1.1 的 **E 口径**）：✅ **已完成**（2026-09-10，A）——该行"`GameConfig` 数值"已改为 **A（读通道）/ F（数值文件与工具）**，E 保留贴图＋音效；
> - **§6.9-4**（README 动画编辑器"已知单位类"）：⏭ **核对后无需改**（`AnimEditorTool.KNOWN_CLASSES` 与文档一致）。
>
> 例外项已在下表**就地标注**（⬜ 未做 / ⏭ 无需改）。

### 6.1 《建筑与怪物机制策划 v1.1》（`docs/建筑与怪物机制策划-v1.0.md`）——**已过时，需整体校准**

| # | 位置 | 现表述 | 应改为 |
| :--- | :--- | :--- | :--- |
| 1 | 文件名 / 表头 | 文件名 `-v1.0`，内容为 v1.1 | 文件名改 `建筑与怪物机制策划-v1.1.md`（或 v1.2），避免版本错配 —— **⬜ 未做**（重命名会影响其他文档引用，待决定） |
| 2 | `:53,74,293` | "塔升级 UI 未开放 / 升级出售 UI 未开始" | 实际 `view/TowerDetailPanel.java` 已实现升级/出售按钮并接入（`GameView.java:428-440`）→ 改为「详情面板已实现；`ITowerSelectionNotifier` 经 controller 的接线未做（见 §7 P1-4）」 |
| 3 | `:51` | "升级链就绪，替换动作未接" | `GameController.upgradeTower` 已改为工厂原位换类（`GameController.java:444-473`）；`*_MASTER` 工厂亦已登记（2026-09-11）→ 改为「L1→L3 全通（见 §7 P1-2）」 |
| 4 | `:97,:198,:305` | 重甲物理减伤 30%「✅ 已确认」 | 补注「**待实现**：契约已确认，代码零实现（见 §7 P0-3）」 |
| 5 | `:99,:198` | 炮塔破甲「对重甲 +30%，无视物理减伤」 | 同上，补「待实现」 |
| 6 | `:209` | 狂暴「速度 +50%，攻击间隔 −30%」 | 补注「速度 +50% 已实现（`BossEnemy.java:56`）；**攻击间隔 −30% 未实现**」 |
| 7 | `:210,:199,:211` | 震地/召唤 | 震地：实体侧已发请求，**`GameController` 未轮询** → 永不生效；召唤：**零实现** → 两者补注「待实现」 |
| 8 | `:128-133,:140` | 炮塔溅射半径 50/65/80 | 补注「**参数未传递**：`Bomb.java:20` 硬编码 60，塔的 `splashRadius` 未被消费 → 实战恒 60」 |
| 9 | `:47,:151` | 兵营士兵 HP 50 / 伤害 8 | 与代码一致（`Barrack.java:48-51`）**保留**；但需与 §6.6-1 的 70 冲突定稿 |
| 10 | `:19-20,:58-60` | Enemy 属性表（HP/速度/赏金） | 增列 **攻击力 / 攻击冷却** 两列（现状 5/1000、4/700、9/1400、22/1100），并注明"当前不可配，见整改 §7 P0-1" |
| 11 | `:284-293` | 落地优先级表 | 按代码现状重排勾选；补 P0 数值通道项 |
| 12 | `:49` | "普通敌人 … HP 80 / 速度 60 / 赏金 10" | 与 `balance.json` 一致，保留 |

### 6.2 《防御塔子系统说明》（`docs/防御塔子系统说明.md`）

| # | 位置 | 现表述 | 应改为 |
| :--- | :--- | :--- | :--- |
| 1 | `:82` | "`upgradeTower(tower)` **现状为数值式**（扣 `getUpgradeCost()` 后 `tower.upgrade()`，level++、伤害×1.3）——与换类升级冲突，本设计将改写" | **已改写完成**：`GameController.java:444-473` 为工厂原位换类，升级费取 `nextLevelSpec.getCost()`；`Tower.upgrade()`/`getUpgradeCost()` 已成为**死代码**（见 §7 P1-6） |
| 2 | `:84` | "`ITowerSelectionNotifier` 仍未接线、**无实现类**" | 实现类**已存在**：`view/TowerDetailPanel.java:30 implements ITowerSelectionNotifier`；但**未经 controller** 接线（`GameController.java:58,88` 字段从不被读）→ 改为「实现类已就绪，controller 侧注线未做（§7 P1-4）」 |
| 3 | `:83` | "`TowerType` 现含 `ARROW / ARROW_ELITE / CANNON / BARRACK`" | 实际 9 值（`TowerType.java:16-26`） |
| 4 | `:202` | "士兵**不分级**（不新建士兵子类）" | 与《友方士兵子系统说明》§1-2 **互斥**：`EliteSoldier` / `RoyalSoldier` 已存在。需定稿：以《友方士兵子系统说明》为准，本句删改 |
| 5 | §12 费用与退款表 | "⏳ 由实现者定义" | 已定稿：升级价 = `nextLevelSpec.getCost()`；新塔 `totalCost` 由下一级塔构造内置（如 `EliteArrowTower.java:28`=125）→ 据实回填 |
| 6 | `:29` | "`upgrade()` 伤害 ×1.3、`sell()` 返还 `totalCost/2` 由基类提供" | `upgrade()` 删除后仅保留 `sell()`；`totalCost/2` 仍有效（`Tower.java:81-84`，`TowerDetailPanel.java:97` 展示） |
| 7 | `:20` 注 | 兵营 5s / 上限 2（升级后 3/4） | 与 `Barrack.java:46-47`（2 / 5000ms）一致，保留；但 `Barrack.java:31` 注释自称「PRD 3秒/个、上限 3」→ **注释过期**，一并修 |
| 8 | §16 待办清单 | 未勾选多项 | 按代码现状重勾（`ITowerUpgrade` + 9 塔类 + 三能力接口已就绪；原位替换已实现；目录拆分已实现） |
| 9 | 全文 | 未提 `*_MASTER` 工厂登记 | ✅ 已补登记（2026-09-11）→ L2→L3 打通（§7 P1-2） |

### 6.3 《接口契约与抽象类说明》（`docs/接口契约与抽象类说明.md`）

| # | 位置 | 现表述 | 应改为 |
| :--- | :--- | :--- | :--- |
| 1 | `:17` | util 子包列表 `map/anim/editor/asset/audio/fx/json` | 补 `balance`，并加一句"数值文件 `resources/config/balance.json` 由 `util.balance` 管理"（《数值配置JSON化规范》§六 建议项） |
| 2 | `:189` | "`upgradeTower` 仍为数值式 `Tower.upgrade()`（×1.3）临时实现，待 A 改为换类原位替换" | 已实现原位换类（`GameController.java:444-473`）；补充"另需补登 `*_MASTER` 工厂" |
| 3 | `:284` | "开发期在 Main 里改即可微调" | 已改为"改 `src/main/resources/config/balance.json`，重启生效"（`Main.java:39-41`）→ 同步 |
| 4 | `:151` | "`Enemy(x, y, hp, speed, goldReward)` … 默认近战 5 / 冷却 1s" | 保留现状描述，但补"(该两项当前不可配，见《整改方案》§7 P0-1)" |
| 5 | `:88` | `ITowerSelectionNotifier` 实现方标"（后续）" | 改为 `view/TowerDetailPanel`（已实现） |
| 6 | `:258` | "渲染叠加层已接入（`util.anim.UnitAnimator` + GameView 敌/友/塔循环 + Main 登记 6 类单位）" | 与代码一致，保留；可补"炮塔/兵营/精英塔未登记" |
| 7 | `:279-282` §5.1 | "GameConfig：集中数值" | 补"数值来自 `config/balance.json`（缺省回退内置默认），运行期唯一 getter 入口不变" |
| 8 | `:41` §1.1 表末 | 「`util/asset/Assets` 贴图、5 个音效接口实现、**`GameConfig` 数值**」归 E | **⬜ 未做**——经裁定：**数值内容/文件/工具归 F**（`balance.json` + `BalanceEditorTool`），Java 读取通道（`BalanceTable`/`GameConfig`）归 A；E 只保留贴图与音效 → 改写该行（属分工口径，留待《整改分工方案》统一处理） |

### 6.4 《友方士兵子系统说明》（`docs/友方士兵子系统说明.md`）

| # | 位置 | 现表述 | 应改为 |
| :--- | :--- | :--- | :--- |
| 1 | `:15` | 1 级 `Soldier` **默认 HP 70** | 代码为 **50**（`Barrack.java:48`），与《建筑与怪物机制策划》`:47,:151` 一致 → 改 70 → 50，并与 §6.1-9 对齐定稿 |
| 2 | `:21` | "`EliteBarrack` 传 100/65/14/700" | 代码 `EliteBarrack.java:24-26` 只改 `soldierHp=80`、`maxSoldiers=3`，速度/攻击/冷却仍 40/8/800，且 `Barrack.java:128` 恒 `new Soldier(...)` → 改为「**未接线**：2/3 级兵营仍产 1 级兵（§7 P1-5）」 |
| 3 | `:73` vs `:188` | §4 标「主动索敌 ✅ 已接线」，§8 待办又列「主动索敌接线（归 A）**未完成**」 | **自相矛盾**。代码已接线（`GameController.java:151-154`）→ §8 该待办勾选 |
| 4 | `:20,150,189` | 三档共用 `soldier.json`、`Main` 已分别登记 `EliteSoldier`/`RoyalSoldier` | 代码 `Main.java:52-63` **只登记了 `Soldier`**；`EliteSoldier`/`RoyalSoldier` 未登记 → 改为「待登记（`Main` 补两行）」 |
| 5 | `:154,191` | 尺寸 `Soldier 32 / EliteSoldier 36 / RoyalSoldier 40` | 与 `sizes.json` 一致，保留 |
| 6 | §8 待办 | 多项已勾选但含上述未达成项 | 按代码现状重勾并补「登记 2/3 档动画」「产 2/3 级兵」两条 |

### 6.5 《视觉尺寸配置规范》（`docs/视觉尺寸配置规范.md`）

| # | 位置 | 现表述 | 应改为 |
| :--- | :--- | :--- | :--- |
| 1 | `:107-125` §四 种子表 | NormalEnemy 20×20 / FastEnemy 16×16 / TankEnemy 30×30 / BossEnemy 46×46 / Soldier 18×18 / 三塔 20×20 | **整体过时**。实测 `assets/sizes.json`：`NormalEnemy 48`、`FastEnemy 44`、`TankEnemy 44`、`BossEnemy 56`、`Soldier 32`、`EliteSoldier 36`、`RoyalSoldier 40`、`ArrowTower/CannonTower/Barrack/Elite* 48`、`Arrow 8`、`Bomb 14` → 按实际重写为「当前基线表」 |
| 2 | `:83,:195` | 「当前应枚举出 **13 个**具体类」「清单与 model 下 13 个具体子类一致」 | 实际 **18 个**（敌人 4 + 友方 3 + 塔 9 + 投射物 2）→ 改为 18；`*_MASTER` 3 类尺寸条目已于 2026-09-11 补齐（§8-3） |
| 3 | `:191` §八 验收 #2 | "删除 6 处 `setWidth/setHeight` 后…**与改造前一致**" | 尺寸已被重调，该验收项**历史性达成但当前不成立** → 改为"改造时一致；现行基线见 §四" |
| 4 | `:129-142` §五 清单 | 只列 8 个实体类的改动 | 已全部完成（各实体类无 `setWidth/setHeight` 残留）→ 勾选完成 |
| 5 | `:96-99` §3.5 | "改尺寸会同步影响碰撞半径与血条" | 保留（与实现一致，`LivingEntity.java:77-79`） |

### 6.6 《关卡波次配置规范》（`docs/关卡波次配置规范.md`）

| # | 位置 | 现表述 | 应改为 |
| :--- | :--- | :--- | :--- |
| 1 | `:140` | "数值（HP/速度/**近战攻击**/冷却/赏金等）归属：由 A 在 `GameConfig` 侧按 id 补齐每类敌人的构造参数（对齐…'数值集中、实体类内不写死'）" | **与代码冲突**：`GameConfig` 只有 HP/速度/赏金，攻击力/冷却写死在实体类 → 两种改法：(a) 采用 §7 P0-1 让攻击力等**确实**进配置，保留本句并加"（待落地）"；(b) 若短期不改，则本句必须删除"近战攻击/冷却"，明确"当前不可配"。**建议 (a)** |
| 2 | `:133-138` §四 词汇表 | 四类敌人接线状态标"待 A 接线" | 已全部接线（`GameController.java:219-248`）→ 更新为"已接入" |
| 3 | `:5,:6` 状态行 | "**已实现**…运行期接线…由 A 在 D 交付后另行集成" | 集成也已完成 → 状态改「全部实现（含运行期集成）」 |
| 4 | `:186` §6.2.2 | "数值取自 GameConfig 按 id 补齐的槽位" | 补注"攻击力/冷却尚未纳入（§7 P0-1）" |
| 5 | §3.2 `maxPrepMs` | 省略 → 取 `GameConfig.waveIntermissionMs` | 与实现一致（`LevelWaves.java:85,97`、`GameController.java:270-277`），保留 |
| 6 | 新增 | — | 补一节"波数唯一来源 = 本表；`balance.json.totalWaves` 仅兜底"（§5 裁定） |

### 6.7 《单位行为动画-渲染层设计》（`docs/单位行为动画-渲染层设计.md`）

| # | 位置 | 现表述 | 应改为 |
| :--- | :--- | :--- | :--- |
| 1 | `:3` 状态行 | "**已实现**" | 保留（`util/anim/UnitPoseResolver`、`UnitAnimator`、`GameView` 叠加、`Main` 登记均已落地） |
| 2 | `:95-99` §7 待办 | 4 项未勾（实现 Resolver / 实现 Animator / GameView 接入 / Main 注册） | **全部已完成** → 勾选；`契约固化标准模式键` 也已完成（本节表格即契约） |
| 3 | `:100` | 死亡态动画「二选一：①加死亡态 ②从各动画表移除 `death`」 | **仍未决** → 需拍板（现状：`UnitAnimator.overlay` 对已死亡单位直接跳过，`death` 帧永不播放） |
| 4 | `:101` | 6 类已注册，炮塔/兵营/精英塔未注册 | 与代码一致（`Main.java:52-63`），保留；但需与 §6.4-4（`EliteSoldier`/`RoyalSoldier` 未登记）交叉核对 |
| 5 | `:62-64` 注册示例 | 示例仅 3 行 | 补实际 6 行清单，避免读者以为只有 3 类 |

### 6.8 《音效扩展方法说明》（`docs/音效扩展方法说明.md`）

| # | 位置 | 现表述 | 应改为 |
| :--- | :--- | :--- | :--- |
| 1 | `:73` | 「士兵出场 … **待友军注册接线（组长后续）**」 | 已接线：`GameController.java:299-308` `addAlly` → `unitSound.onUnitSpawned(ally)` → 标"已完成" |
| 2 | `:74-76` | 漏怪扣生命 / 金币不足 需"交互侧调用" | 核查是否已落地；如未落地保留为待办（`IStatusObserver` 回调内判断新旧生命 / `placeTower` 返回 false 分支） |

### 6.9 `README.md`

| # | 位置 | 现表述 | 应改为 |
| :--- | :--- | :--- | :--- |
| 1 | `:13-14` | "启动时会自动读取路线文件 `src/main/resources/maps/default_path.json`" | **路径已废弃**：现为 `maps/<key>/path.json`（§"地图分包与文件结构"节已写对，顶部此句残留旧路径）→ 删除或改写 |
| 2 | `:5` | "抵御10波敌人" | 已改为「抵御**多波**敌人进攻（波数与组合由 `maps/<key>/waves.json` 唯一决定）」+ 一句"当前为测试用例数据、非最终关卡设计"——**不写死波数**（按 §5-6 裁定） |
| 3 | `:26-33` 工具表 | 缺「数值配置」一行 | 补 `com.kingdom.game.util.editor.BalanceEditorTool` |
| 4 | `:89` | 动画编辑器"已知单位类（敌人 Normal/Fast/Tank/Boss、友方 Soldier、塔 ArrowTower）" | 补精英士兵三档 / 炮塔 / 兵营 —— **⏭ 核对后无需改**：`AnimEditorTool.KNOWN_CLASSES`（`:62-65`）= enemies 4 类 / allies `Soldier` / towers `ArrowTower`，与文档**完全一致**（要扩下拉清单应改工具代码，而非文档） |

---

## 7. 代码整改清单

### P0-1 敌人「攻击力 / 攻击冷却」纳入配置（**最高优先级**）

> ✅ **执行状态（2026-09-11 闭环）**：第 4 步（实体类去写死）**已完成**——4 个敌人构造扩参为
> `(x, y, hp, speed, goldReward, attackDamage, attackCooldownMs)` 并**保留老 5 参重载**；
> 老重载从 `balance.json` 取值（`BalanceTable.runtime()`），**实体类内 8 个写死值已全删**（含 `Enemy` 基类的 5/1000）。
> 8 键**已写入 `config/balance.json`**，**实战已生效**。
> ⏳ **仍待**：第 3 步 `spawnEnemy` 显式传参（**5-1，归 A**）——改用新签名即可，不改也已生效（老重载读 JSON）；
> `BalanceEditorTool` 输入框补这 8 键（F-1 剩余，否则工具保存会按默认值写回）。
> ⚠️ **与目标路径的偏离**：原目标写的是 `balance.json` → `BalanceTable` → **`GameConfig`** → `spawnEnemy` 注入；
> 实际当前走 `balance.json` → `BalanceTable.runtime()` → 敌人构造期（**绕过 `GameConfig`**），原因同 §7 P1-1
> （实体不持有 `GameConfig` 引用）。`GameConfig` 的 8 个 getter 已就绪，5-1 落地时即可回归原定路径。
> 生效口径：**改 `balance.json` → 重启 → 生效**（已验证）。
> ℹ️ 第 9 键 `tankPhysicalReduction` **有意未写入**——其实体侧消费（B-5）未实现，写入会形成"假通道"（§7 P0-2）。

**问题**：契约要求数值集中、实体类不写死（《关卡波次配置规范》`:140`、《接口契约》§1），但攻击力/冷却既不在 `balance.json`、也不在 `GameConfig`，只写死在四个实体类里；改一次攻击力必须改 Java 重编译。

**目标**：攻击力/冷却改为 `balance.json` → `BalanceTable` → `GameConfig` → `spawnEnemy` 注入。

**字段表草案（`config/balance.json` 追加 8 键，扁平风格与现有 15 键一致）**：

| 键 | 类型 | 合法范围 | 默认（= 当前代码值） |
| :--- | :--- | :--- | :--- |
| `normalAttackDamage` | number | ≥1 整数 | 5 |
| `normalAttackCooldownMs` | number | ≥1 整数 | 1000 |
| `fastAttackDamage` | number | ≥1 整数 | 4 |
| `fastAttackCooldownMs` | number | ≥1 整数 | 700 |
| `tankAttackDamage` | number | ≥1 整数 | 9 |
| `tankAttackCooldownMs` | number | ≥1 整数 | 1400 |
| `bossAttackDamage` | number | ≥1 整数 | 22 |
| `bossAttackCooldownMs` | number | ≥1 整数 | 1100 |

**步骤**：
1. `util/balance/BalanceTable.java`：增 8 个字段（`normalAttackDamage` 等）+ getter/setter；`FIXED_KEYS`（`:151-156`）追加 8 键；`defaults()`（`:68-91`）填上表默认；`toJson()`（`:178-217`）与 `fromJson()`（`:242-282`）按现有 `readInt(...)` 模式补齐；**✅ 已完成**（2026-09-10；行号已随改动位移，勿再按本行原行号定位）；
2. `config/GameConfig.java`：增 8 个 getter（+ 可选的 fluent setter），构造器 `:98-122` 从 `BalanceTable` 读取；**✅ 已完成**（只加 getter；写入通道仍是 `balance.json`/`BalanceTable`，新增字段无字面量默认，符合 §8-7 红线）；
3. `controller/GameController.java:219-248` `spawnEnemy`：四类敌人都改为传 5 个数值（hp / speed / goldReward / attackDamage / attackCooldownMs）；**⏳ 待 B-4 落地后由 A 补**；
4. 实体类：`Enemy`、`FastEnemy`、`TankEnemy`、`BossEnemy` 各增一个全参构造（含 `attackDamage, attackCooldownMs`），**保留旧签名重载**转调默认值（避免破坏既有调用与测试）；**⏳ B-4**；
   - `LivingEntity`（`:23-25`）目前无 `attackDamage/maxAttackCooldown` 的 setter，构造注入需由子类赋 `protected` 字段（`Enemy` 现有 `this.attackDamage = 5` 的写法即是先例，`:33-34`）；
   - 删除 `Enemy.java:33-34` 的默认赋值 + 各子类的写死值，改为构造参数。
5. **回归口径**：首版 JSON 值 = 上表默认 → 改造后战斗表现与改造前**完全一致**。

**验收**：① 改 `balance.json` 的 `bossAttackDamage` → 重启 → Boss 打士兵的伤害随之变化；② 删除该键 → 回退默认 22 并打印 `[BalanceTable]` 告警；③ 全仓库搜不到 `= 5;`/`= 22;` 之类的敌人攻击力写死值。

---

### P0-2 堵死「假配置通道」（**与 P0-1 同批做**）

> ✅ **已完成（2026-09-10）**：采纳 (a) 告警 + 保留——`fromJson` 未知键分支已打印 `[BalanceTable] 未知字段 "…" 已忽略（运行期不读取，请勿在此写无对应实现的数值）`，`BalanceEditorTool` 的「自定义变量」区已加醒目提示（`customWarnLabel`）。
> 注：文中 `:274-279`/`:201-211`/`:327-355`/`:383-394` 等行号已随本轮改动位移。

**问题**：`BalanceTable.fromJson` 把**任何未知键静默收进 `extra`**（`BalanceTable.java:274-279`），`toJson()` 原样写回（`:201-211`），`BalanceEditorTool` 还提供「自定义变量」增删入口（`:327-355`、`:383-394`），但**运行期没有任何代码读 `extra`**（全仓库仅编辑器引用）。结果：在 `balance.json` 写 `"normalAttackDamage": 8` 会保存成功、零告警、毫无效果 —— 比"不能配"更具误导性。

**步骤（二选一，建议 (a)）**：
- **(a) 告警 + 保留（低风险）**：`fromJson` 未知键分支改 `System.err.println("[BalanceTable] 未知字段 \"" + key + "\" 已忽略（运行期不读取，请勿在此写无对应实现的数值）")`，再入 `extra`；`BalanceEditorTool` 的「自定义变量」区加醒目提示"⚠ 这些字段运行期**无人读取**，仅随文件保存"。
- **(b) 直接下线（最干净）**：删除 `extra` 机制与编辑器的自定义变量区；`fromJson` 遇未知键改为**告警且丢弃**。
- 若采纳 P0-1，则 P0-1 完成后 `extra` 对"攻击力"类字段的诱惑自然消失，本条主要防未来复发。

**验收**：在 `balance.json` 手写一个无实现字段 → 启动控制台出现 `[BalanceTable]` 告警；编辑器保存时不静默接受。

---

### P0-3 重甲物理减伤 30% + 炮塔破甲（契约已"✅ 已确认"，代码零实现）

> 🟡 **执行状态（2026-09-10）**：**A 侧通道已落地**——`BalanceTable` 增 `tankPhysicalReduction`（默认 0.3，`readRatio` 强制 **[0,1)**，越界告警回退），`GameConfig` 增对应 getter。
> ⏳ **仍待**：`balance.json` 取值（F-2）、`Enemy.damageTakenMultiplier` 与 `Arrow`/`Bomb.onHit` 减伤/破甲（B-5）、炮塔破甲参数（C）。

**问题**：`Arrow.onHit`（`Arrow.java:31-38`）与 `Bomb.onHit`（`Bomb.java:40-50`）直接 `takeDamage(damage)` 全额扣血，无护甲概念；《建筑与怪物机制策划》`:97,:99,:198,:305` 已确认 30% 减伤与炮塔破甲。

**目标**：把减伤做成**可配 + 可覆盖**的机制，在投射物命中处落地。

**步骤**：
1. `balance.json` 增 `tankPhysicalReduction`（默认 0.3，范围 [0,1)）；`BalanceTable`/`GameConfig` 同步；
2. `Enemy` 增 `damageTakenMultiplier` 概念（默认 1.0）：`TankEnemy` 构造按配置置 `1 - reduction`；或更通用地：`Enemy` 增 `physicalReduction` 字段 + `takeDamage` 的重载/`computeDamage(int raw, boolean ignoresArmor)`；
3. `Projectile` 增"是否无视减伤"标记（箭 = 否；弹 = 是，即炮塔破甲；并可按策划 §2.3 给炮塔 +30% 加成，做成 `CannonTower` 参数）；
4. `Arrow.java:33` 改为 `targetEnemy.takeDamage(damage)` 走减伤；`Bomb.java:44` 走无视减伤路径；
5. 飘字显示**实际伤害**（当前显示 `damage` 原始值，`:35-36` / `:46`）。
6. 文档同步：`建筑与怪物机制策划` §2.1/§3.1/§4 的"✅ 已确认"改为"✅ 已实现"。

**验收**：箭塔打重甲伤害 = 面板值 ×0.7；炮塔打重甲不被减伤（且可加 30% 破甲）；飘字与实际扣血一致。

---

### P0-4 塔 / 士兵数值零通道（**二期**，本期先登记方案）

**问题**：`ArrowTower.java:30-48`、`CannonTower.java:28-49`、`Barrack.java:36-77` 及 `Elite*`/`Master*` 的造价/伤害/射程/冷却/溅射/士兵参数全是类内常量或构造赋值；`BalanceTable` 无任何 tower 字段。策划 §2.2–2.4 已给出精确数值表，但数值方无自助通道。

**方案草案（`balance.json` 分组字段，二期实施）**：
```
"towerArrow":   { "cost": 50,  "damage": 18, "range": 120, "cooldownMs": 550 },
"towerArrow2":  { "cost": 75,  "damage": 27, "range": 140, "cooldownMs": 550 },
"towerArrow3":  { "cost": 120, "damage": 41, "range": 140, "cooldownMs": 440, "chainTargets": 1, "chainDamageRatio": 0.5 },
"towerCannon":  { "cost": 80,  "damage": 45, "range": 140, "cooldownMs": 1500, "splashRadius": 50 },
...
"towerBarrack": { "cost": 100, "maxSoldiers": 2, "spawnIntervalMs": 5000,
                  "soldierHp": 50, "soldierSpeed": 40, "soldierAttack": 8, "soldierCooldownMs": 800 },
```
> 注意：`BalanceTable` 现为**顶层扁平对象**（规范 §3.1 明确"不支持嵌套分组"）。采用分组需要同步修订该条规范，或改用扁平前缀键（`arrowDamage` 等）保持扁平约定。**建议保持扁平**，与本项目既有风格一致。

**前置条件**：需先完成 P1-6（删死代码）与 P0-2（防假通道），否则新增 tower 字段仍可能被 `extra` 静默收纳。

**迁移顺序**：① 先只搬"塔造价"（与 `TowerSpec.cost` 天然对齐，改动最小）；② 再搬伤害/射程/冷却；③ 最后搬士兵参数与溅射半径。

---

### P1-1 溅射半径传递（策划数值当前 100% 无效）　✅ **已闭环 2026-09-11**

- **问题**：`CannonTower.splashRadius`(50/65/80) 从未被消费（`getSplashRadius()` `CannonTower.java:53` **全仓库无调用方**）；实际溅射恒为 `Bomb.java:20` 的 `SPLASH_RADIUS = 60`。
- **步骤**：`Bomb` 构造（`:30-33`）扩参 `double splashRadius`，字段化替换常量；`CannonTower.attack(...)` 生成 `Bomb` 时传 `splashRadius`；`EliteCannonTower.java:25` / `MasterCannonTower.java:27` 已覆写该字段，自动生效；`Bomb.onHit:43` 改用字段。
- **验收**：1/2/3 级炮塔实测溅射半径分别 50/65/80；`Bomb` 内无硬编码半径。

- **实际交付（2026-09-11，B-1 + C-2 同批）**：
  - `Bomb`：构造末位扩参 `double splashRadius`，`SPLASH_RADIUS = 60` **已删除**，`onHit()` 改用字段，并加 `getSplashRadius()`；新增 `splashRadius` 无任何内置常量。
  - `CannonTower.attack()`：把 `splashRadius` 作为第 7 个实参传入 `Bomb`。
  - **超出原步骤的追加**：三档半径改由 `config/balance.json` 驱动（⑧ 组 3 键
    `cannonSplashRadius`/`eliteCannonSplashRadius`/`masterCannonSplashRadius`，默认 50/65/80），
    塔类原先写死的 `= 50/65/80` 已删 → **改半径不再需要动 Java**（需求方要求"提供 JSON 接口给其他人随时改"）。
  - 消费链路：`balance.json` → `BalanceTable.runtime()`（只读懒加载单例）→ 塔构造期 → `Bomb` 构造 → `onHit()` 判定。
- **自验数据（2026-09-11）**：
  - JSON 置 50/65/80 → 三塔读出 50/65/80，`Bomb` 80；
  - JSON 置 137/170/210 → 三塔读出 **137/170/210**（证明确由 JSON 驱动，非内置默认）；
  - 键置 `0` / 写成 `"abc"` / 整键删除 → 分别告警回退 50、回退 80、静默回退 65，**不抛异常**；
  - `mvn -o compile` 通过；`mvn -o javafx:run` 正常启动窗口，日志无异常、无 `[BalanceTable] 未知字段` 告警。
- ⚠️ **遗留（需 A / F 处理）**：
  1. 本批改了 `util.balance.BalanceTable`（A 侧数值数据类），**需 A 追认**；偏离 §九「唯一运行期入口 = `GameConfig`」，理由见《数值配置JSON化规范》§3.2 末注。
  2. `BalanceEditorTool` 输入框**尚未**加这 3 键 → 下次用该工具保存会把 3 键按**内置默认值**写回（同 F-1 的既有风险）。
  3. `getSplashRadius()` 现已可从 `Bomb` / 塔两侧读到，但**仍无 UI 调用方**（`TowerDetailPanel` 类注释明确"不 import 具体塔类、不做 instanceof 分派"，故未接线）→ P1-6 死代码项对这 2 个 getter 仍成立。

### P1-2 三级升级链补登记（L2→L3 运行期断链）

- **问题**：`Main.java:89-91` 只登记 `ARROW_ELITE / CANNON_ELITE / BARRACK_ELITE` 三个工厂；三个 `*_MASTER` **既不在菜单也无工厂** → 2 级塔升 3 级时 `GameController.java:457-461` 取 `factory == null`，提示"下一级尚未开放"。塔类本身已齐备（`MasterArrowTower` / `MasterCannonTower` / `MasterBarrack`）。
- **步骤**：`Main` 追加三行 `registerUpgradeFactory(TowerType.ARROW_MASTER, MasterArrowTower::new)` 等；确认 `TowerType.java:19,22,25` 常量已存在（已存在，无需改枚举）。
- **联动**：必须同时做 §8-3（`sizes.json` 补 Master 条目），否则三级塔会掉到 `default` 20×20，比 2 级（48×48）还小。
- **验收**：1 级箭塔连续升级两次 → 获得 `MasterArrowTower`、尺寸 48、满级后升级按钮置灰。
- ✅ **已完全落地（2026-09-11）**：`Main` 三行工厂已登记（2026-09-10）→ L2→L3 工厂链路已通；**§8-3 联动项已补**（`sizes.json` 三条 Master 尺寸 48×48）→ 尺寸 48 的验收点**已满足**，L3 不再回退 20×20。

### P1-3 Boss 三技能补完

> 🟡 **执行状态（2026-09-10）**：**震地的控制层侧已完成**——`GameController.update()` 已加 `settleBossStompRequests()`：遍历敌人，`BossEnemy.consumeStompRequest()` 为真则对全部 `towers` 调 `t.stun(3000)`（常量 `BOSS_STOMP_STUN_MS`）。
> ⏳ **仍待**：震地的 **"狂暴后每 15s"计时**（B-2；现状仅半血狂暴触发一次）、狂暴**攻击间隔 −30%**（B-2）。召唤已按 Q2 关闭（不做）。
> ⚠️ 下表行号为**改造前快照**，已随 P1-6 死代码删除与本轮改动位移（`Tower.stun()` 现行号 `77-80`，`BossEnemy` 计时/请求 `56-70`），勿按原行号定位。

| 技能 | 现状 | 步骤 |
| :--- | :--- | :--- |
| 震地（眩晕全塔 3 秒） | ✅ **控制层已接线**（`GameController.settleBossStompRequests()`）；⏳ `BossEnemy` 的"每 15s"计时未做（现仅半血狂暴触发一次） | ~~在 `GameController.update()` 轮询~~（已完成）；剩余：`BossEnemy` 内记录上次震地时刻，狂暴后每 15s 重置 `stompRequested` |
| 狂暴「攻击间隔 −30%」 | 只做了速度 ×1.5（`BossEnemy.java:56`） | `triggerEnrage()` 内再加 `maxAttackCooldown = (int)(maxAttackCooldown * 0.7)` |
| 召唤 2 只普通敌人 | **零实现**（全仓库无 summon 逻辑） | **⏸ 已裁定不做**（Q2 关闭，见《分工方案》§6-①） |

### P1-4 `ITowerSelectionNotifier` 接线（契约链路当前被绕过）

- **问题**：`GameController.java:58` 有字段、`:88` 有 setter，但**全仓库无人调用 `setSelectionNotifier`**，字段赋值后从不被读取；实际由 `GameView.java:428-440` 直接 `detailPanel.onTowerSelected(t)` —— UI 直连，绕过控制层，与《接口契约》"UI 只经接口"的数据流规则不符。
- **步骤（二选一，需拍板）**：
  - **(a) 补线**：`Main` 注入 `controller.setSelectionNotifier(detailPanel)`；`GameController` 在"点中塔/取消"时回调（需 UI 侧把点击事件经接口交给 controller，或 controller 暴露 `selectTower(Tower)`/`clearSelection()`）；`GameView` 改为只调用 controller；
  - **(b) 修订契约**：承认"选中通知由 UI 层内部直连（`GameView` ↔ `TowerDetailPanel` 同属 UI 层，不涉后端）"，则**删除** `GameController` 中的 `selectionNotifier` 字段/setter 与《接口契约》§2.2 相应行，避免"有字段无接线"的悬空契约。
- **建议 (b)**：选中态是纯 UI 状态，交给 controller 反而增加耦合；删除悬空契约成本最低。
- **文档同步**：无论选哪个，`防御塔子系统说明.md:84` 与 `接口契约:88` 都要按结果改写。

### P1-5 兵营产出 2/3 级士兵

- **问题**：`Barrack.java:128` 恒 `new Soldier(...)`；`EliteBarrack` / `MasterBarrack` 无产出覆写 → 2/3 级兵营仍产 1 级兵（速度/攻击/冷却也不随等级提升）。
- **步骤**：`Barrack` 抽 `protected Ally createSoldier()`（返回 `new Soldier(x, y, soldierHp, soldierSpeed, soldierAttack, soldierCooldown)`）；`EliteBarrack` 覆写返回 `EliteSoldier`，`MasterBarrack` 返回 `RoyalSoldier`（二者的全参构造签名与 `Soldier` 一致，见《友方士兵子系统说明》§5.2）。
- **注意**：必须在兵营侧生产，不能在 `allySink` 事后替换，否则兵营自己的槽位计数与战场实际不符。
- **联动**：需与 §6.4 的"士兵 HP 50 vs 70"定稿一致（`EliteBarrack.java:25` 现为 80）。

### P1-6 `Tower` 死代码清理 + `level` 恒 1（**你点名要求修复**）

> ✅ **已完成（2026-09-10）**：采纳 **(a) 彻底删除 + (i) 类身份表达级别**。
> - `Tower.upgrade()` / `Tower.getUpgradeCost()` / `Tower.upgradeCostBase` **及其 7 处赋值**已全部删除；
> - `level` 字段已删；改由 `ITowerUpgrade.getLevel()` + 各塔类 `public static final int LEVEL`（1/2/3）表达，`TowerDetailPanel` 已恢复等级显示（验收点 ③ 满足）。
> - 文档联动已按下方清单同步（《防御塔子系统说明》《接口契约》《数值配置JSON化规范》）。
> - ⚠️ 下表行号为**改造前快照**（`Tower.java:23/25/73-78/93-94` 等已失效，现行 `Tower.java` 同位置为 `stun` 相关字段），保留仅为留痕。

**问题**：以下成员**全仓库零调用**（`grep` 已确认）：

| 成员 | 位置 | 状态 |
| :--- | :--- | :--- |
| `Tower.upgrade()`（伤害 ×1.3、升级成本 ×1.2） | `Tower.java:73-78` | 死代码；且与"换类升级"语义冲突 |
| `Tower.getUpgradeCost()`（= `upgradeCostBase * level`） | `Tower.java:94` | 死代码 |
| `Tower.upgradeCostBase` 字段 | `Tower.java:25` + 7 处赋值：`ArrowTower.java:46`(45)、`CannonTower.java:47`(50)、`Barrack.java:71`(60)、`EliteArrowTower.java:27`(60)、`EliteCannonTower.java:26`(70)、`MasterArrowTower.java:29`(70)、`MasterCannonTower.java:28`(90) | 死字段；取值 45/50/60/70/90 与各族真实升级费 `UPGRADE_COST`(75/120/100/150/180/250) **完全不是一回事**，语义重复且不一致 |
| `level` 字段 | `Tower.java:23`，仅 `upgrade():75` 递增、`getLevel():93` 读取（**无调用方**） | 恒为 1；`TowerDetailPanel.java:93-94` 因此**注释掉了等级显示**（"待 C 在三个精英塔构造补 `this.level = 2` 后可恢复"） |

**修复方案（推荐 a）**：
- **(a) 彻底删除**：删除 `upgrade()`、`getUpgradeCost()`、`upgradeCostBase` 字段及 7 处赋值。理由：升级已完全走 `ITowerUpgrade` + `nextLevelSpec.getCost()`（`GameController.java:452-462`），升级费唯一来源已是 `TowerSpec`；留着只会再次被误用为"数值式升级"。
- (b) 保留 API 但改为读 `nextLevelSpec`：`getUpgradeCost()` 返回 `nextLevelSpec == null ? -1 : nextLevelSpec.getCost()`，`upgrade()` 直接删除（换类语义下无意义）。适合无法一次性改动调用方的场景——当前无调用方，故不需要。
- **`level` 处理（二选一）**：
  - **(i) 由类身份表达级别**（推荐）：给 `ITowerUpgrade` 增 `int getLevel()` 或复用塔类常量（`ArrowTower.LEVEL=1` / `EliteArrowTower.LEVEL=2` / `MasterArrowTower.LEVEL=3`），删掉 `Tower.level` 字段；`TowerDetailPanel` 经接口显示等级；
  - (ii) 各精英/大师塔构造内 `this.level = 2/3`（最小改动，但"等级"仍是可变字段，与"每级是独立类"的设计意图相悖）。

**改动联动（必须同步的文档）**：
- `防御塔子系统说明.md:29`（"`upgrade()` 伤害 ×1.3 … 由基类提供"）、`:82`（数值式升级现状）；
- `接口契约与抽象类说明.md:167`（"`tryAttack(...)`：冷却后索敌开火…`upgrade()` 伤害 ×1.3；`sell()` 返 50% 投入"）、`:189`；
- `数值配置JSON化规范.md:205`（红线里提到"`Tower.upgrade()` 的 ×1.3/×1.2 规则"）——红线措辞需重写为"不动升级链与精英塔"。

**验收**：① 全仓库搜不到 `upgradeCostBase` / `getUpgradeCost` / `Tower.upgrade`；② 1→2→3 级升级链仍按 `nextLevelSpec.getCost()` 正确扣费；③ 详情面板能显示"Lv.2/Lv.3"（若采纳 (i)）；④ 无新增编译警告。

### P1-7 `showBossWarning()` 空实现

- **问题**：`IScreenFx.showBossWarning()` 是契约方法（`IScreenFx.java:13`），`GameController.java:340-342` 在**最终波每局都会调用**，但 `GameView.java:727-729` 只有 `// TODO Day8：Boss 登场预警` 空体 → 预警永久无表现。
- **步骤**：`GameView` 实现（红字横幅"BOSS 来袭"数秒 + `flashScreen`/`shakeScreen`），复用现有 `drawFloatingTexts`/`drawScreenFlash` 时间驱动写法。
- **验收**：进入最终波可见预警；非最终波不触发。

### P1-8 士兵锁定目标后不进入待命（`idle` 实际不可达）

- **问题**：动画层 `UnitPoseResolver.resolve()`（`UnitPoseResolver.java:40-48`）只在"本帧无位移"时产出 `idle`；而 `GameController.update()` 每帧 `a.engage(a.findTarget(enemies))`（`GameController.java:155-158`），`Ally.engage()`（`Ally.java:36-39`）为**粘性锁定**（目标活着就不换、不清），`Soldier.move()`（`Soldier.java:84-109`）对有活目标持续走位、仅超过 `LEASH_RANGE=150`（`Soldier.java:45`）才放弃 → 士兵常年 `walk`/`attack`。`Soldier.findTarget()` 视野 `VISION_RANGE=90`（`Soldier.java:23`），兵营驻守点就在路径上，一波内几乎总有敌人落在视野内 → **`idle` 分支实际不可达**，《单位行为动画-渲染层设计》§6 验收第 3 条"友方待命→`idle`"不成立。
- **步骤**：在 `Soldier` 侧，目标离开视野（`dist > VISION_RANGE`）即释放目标（`target = null`）→ 走 `returnHome()` 回防待命。**不改抽象基类 `Ally`**（红线：抽象基类仅 A 可改）；若确需调整 `engage` 的粘性语义，转 A 拍板。
- **联动**：`UnitAnimator.hasOverlay()` 把 `moved` **写死为 `true`**（`UnitAnimator.java:82`），用它作"是否跳过静态渲染"的判断，与实际模式不自洽 → 一并改为按实际位移解析；`单位行为动画-渲染层设计.md` §6 验收第 3 条由 D 复核。
- **验收**：士兵无活目标且已到岗时播放 `idle` 帧循环；有目标靠近时 `walk`/`attack` 行为不变；无回归。

### P1-9 兵营升级 / 出售不清理其产出的士兵

- **问题**：`GameController.upgradeTower()`（`GameController.java:462-490`）原位替换后仅 `tower.destroy()`（`:486`），旧 `Barrack` 产出的士兵仍留在共享 `allies` 表；`sellTower()`（`:492-500`）同样遗漏。`Barrack` 只在私有 `slots[].soldier` 持有引用（`Barrack.java:64,70-73`）且**无对外取兵接口**、无反向 owner → 升级后旧兵不死、新营续产，场上士兵数**超上限**（1 级：旧 2 + 新 2 = 4），旧兵还带着旧驻守点残留。
- **步骤**：C 在 `Barrack` 增"交出所产士兵"出口（如 `public List<Ally> getSoldiers()` 或 `releaseSoldiers()`，返回非空 `slot.soldier`；`EliteBarrack` / `MasterBarrack` 继承即得）；A 在 `upgradeTower` / `sellTower` 里 `allies.removeAll(...)`。遵守契约"实体不碰全局列表"——**由兵营返回、Controller 删除**。可选：抽 `ISoldierSource` 接口收敛 `instanceof`。
- **验收**：升级或出售兵营后旧士兵立即从战场移除；新兵营按自身上限重新产出；场上该营士兵总数不超当前等级上限。

### P2 塔 / 士兵数值二期 JSON 化

见 P0-4。**执行前提**：P0-2 已堵假通道、P1-6 已删死代码、`TowerSpec`/`Barrack` 字段已成唯一数值源。

---

## 8. 数据与资产整改清单

| # | 问题 | 证据 | 整改 |
| :--- | :--- | :--- | :--- |
| 1 | ~~`maps/default/waves.json` 只有 9 波、且第 1 波是 `boss_enemy × 2`，与《建筑与怪物机制策划》§3.3 十波表、`README.md:5`「抵御10波」不符~~ → **已裁定：非缺陷**：波次表是**测试用例**数据（联调用），**不是最终关卡设计** | `maps/default/waves.json:1-67` | **不设固定波数、不做十波表重做、本轮不改数据**。仅需保证文档/README **不声称固定波数**（README:5、策划 §3.3 已按"测试用例数据 / 设计目标"改写） |
| 2 | `elite_arrow_tower.png` / `elite_cannon_tower.png` / `elite_barrack.png` **存在但 `AssetKey` 无对应 key**；精英/大师塔 render 复用基础塔 key（`EliteArrowTower.java:35`、`EliteBarrack.java:33`、`EliteCannonTower.java:34`）→ 三张图**永远加载不到**（死文件） | `AssetKey.java:11-23` ↔ `src/main/resources/assets/*.png` | `AssetKey` 补 `ELITE_ARROW_TOWER` / `ELITE_CANNON_TOWER` / `ELITE_BARRACK` 三键，并在对应塔类 render 改用它（`Master*` 无专属图，仍复用 ELITE 或基础 key，需明确约定） |
| 3 | ~~`assets/sizes.json` **缺 3 个 Master 塔条目** → 一旦补上 Master 工厂（P1-2），三级塔会回退 `default` 20×20，**比 2 级 48×48 还小**~~ → ✅ **已补** | `assets/sizes.json:15-17` | ✅ **已补（2026-09-11）**：`MasterArrowTower` / `MasterCannonTower` / `MasterBarrack` 各 48×48 |
| 4 | `AssetKey.ELITE_SOLDIER` / `ROYAL_SOLDIER` 有 key 但 `elite_soldier.png` / `royal_soldier.png` **缺失** → 回退色块 | `AssetKey.java:17-18` | 归 E：补两张贴图，或明确"由动画帧替代、色块为最终回退"并写进文档 |
| 5 | `Main` 未登记 `EliteSoldier` / `RoyalSoldier` 的动画（` soldier.json` 已有关联帧） | `Main.java:52-63` | 补两行 `registerUnitAnimation("allies", "EliteSoldier", "/assets/animations/allies/soldier.json")` 等（与《友方士兵子系统说明》§8 验收项一致） |
| 6 | `Main` 未登记 `CannonTower` / `Barrack` / `Elite*` / `Master*` 的动画；`assets/animations/towers/` 仅有 `arrow_tower.json` | `Main.java:52-63`、`assets/animations/towers/` | 属"未提供素材"，非缺陷；在《单位行为动画-渲染层设计》§7 保持"待补"并列出缺口清单即可 |
| 7 | 0 字节残留占位文件（另含根目录垃圾文件 `nul`） | `src/main/resources/占位.txt`、`src/test/java/占位.txt`、`nul` | ✅ **已删除**（2026-09-10，共 3 个文件） |

---

## 9. 分期与分工

> **分工（谁做什么）见独立文件 `docs/整改分工方案-v1.0.md`**——该文件按六人角色（A~F）逐项标注
> 「主责 / 落文件方」两栏，并以本文档 §7/§8 的条目为唯一出处。本节仅保留**分期视角**，不再重复归属列。

| 期 | 内容 | 归属 |
| :--- | :--- | :--- |
| **一期（P0）** | §7 P0-1 敌人攻击力/冷却入 JSON；P0-2 堵假配置通道；P0-3 减伤/破甲 | A（`BalanceTable`/`GameConfig`/`GameController`）+ B（`Enemy`/`Arrow`/`Bomb`）+ **F（`balance.json` 值与编辑器）**<br>（原写 E，2026-09-10 按《接口契约》§1.1 新口径更正：E 仅贴图/音效） |
| **一期（P1）** | P1-6 `Tower` 死代码清理；P1-2 Master 工厂 + §8-3 尺寸；P1-7 Boss 预警；§8-2 AssetKey 三键 | C（塔类）+ A（`Main`/`GameController`）+ D（`GameView`）+ E（资产） |
| **二期** | P1-1 溅射半径；P1-3 Boss 震地/召唤；P1-4 选中通知（定稿后）；P1-5 兵营产高级兵；**P1-8 士兵待命态（idle）**；**P1-9 兵营升级/出售清理士兵** | B/C + A + D |
| **三期** | P0-4 塔/士兵数值 JSON 化 | A + C + B |
| **文档** | §6 全部 9 份文档逐条修订；新增/固化本文件 §3 数值归属总表 | A（统一执行，避免多人并行改文档） |
| **验收** | §10 | F（测试） |

---

## 10. 验收标准（Definition of Done）

> 📌 **第 2 批执行记录（2026-09-10，A 侧）**：本清单中 **A 可独立完成的验收点已通过**——
> 第 1 条 ✅（82 源文件 `javac 17`、0 error，仅既有 unchecked 注记）；第 6 条 ✅（`upgradeCostBase`/`getUpgradeCost`/`Tower.upgrade` 全无残留，详情面板显示等级）；
> 第 3 条 ✅（未知键告警实测触发，含 `extra` 保留）；第 2 / 4 条 🟡（通道 + 校验已实测：非法冷却 0 与减伤 1.5 均告警回退，但**伤害生效链路待 B-4**）；
> 第 5 条 🟡（工厂链已通，**尺寸 48 待 E-2**）；第 9 条 ⬜（D 待做）。
> 复核方式：`BalanceTable.defaults() → toJson() → fromJson()` 往返一致（9 新键 + 旧键全等）；非法值（冷却 0、减伤 1.5）告警回退默认。

1. `mvn -q compile`（或 `javac`）通过，无新增警告；
2. **P0-1**：改 `balance.json` 的任一 `*AttackDamage` / `*AttackCooldownMs` → 重启 → 战斗伤害/频率随之变化；删除该键 → 回退默认并告警；全仓库搜不到敌人攻击力的写死值；
3. **P0-2**：`balance.json` 手写无实现字段 → 启动有 `[BalanceTable]` 告警；编辑器不静默接受；
4. **P0-3**：箭塔打重甲 = 面板值 ×0.7；炮塔不受减伤；飘字与实际扣血一致；
5. **P1-2**：1 级塔连续升两级可达 `Master*Tower`，尺寸 48×48，满级后升级按钮置灰；
6. **P1-6**：全仓库搜不到 `upgradeCostBase` / `getUpgradeCost` / `Tower.upgrade`；升级链扣费仍正确；详情面板显示等级；
7. **P1-1** ✅ **已自验通过 2026-09-11**：1/2/3 级炮塔溅射半径分别 50/65/80，`Bomb` 内无硬编码；
   改 `balance.json` ⑧ 组 3 键（试验值 137/170/210）→ 三塔读出同为 137/170/210；非法值（0 / `"abc"` / 删键）告警回退默认不崩；
   ⏳ **待 F 复验**：进游戏后按《防御塔子系统说明》确认实战溅射范围随 JSON 变化（本项尚无 UI 读数，需目视弹着范围）；
8. **P1-3**：Boss 狂暴后每 15 秒全塔眩晕 3 秒；狂暴攻击间隔 −30%；召唤按 Q2 决议；
9. **P1-7**：进入最终波可见 Boss 预警；
10. **P1-8**：士兵无活目标且已到岗时播放 `idle` 帧循环（不再常年 `walk`/`attack`）；有目标靠近时 `walk`/`attack` 不变；
11. **P1-9**：升级或出售兵营后旧士兵立即从战场移除；新兵营按自身上限重新产出，场上该营士兵总数不超当前等级上限；
12. **数据/资产**：三张精英塔贴图生效；`sizes.json` 覆盖全部 18 个具体类。**（波次数据不设验收标准**——波次表为测试用例数据，由 F 按游戏性需要维护，见 §5-6/§8-1）
13. **文档**：§6 清单逐条勾选完毕；`docs/` 内不再出现互相矛盾的口径（士兵 HP、士兵是否分级、upgradeTower 现状、13 vs 18 个具体类、波数来源）。

---

## 11. 范围红线（不做清单）

- 不新增第二套数值 / 尺寸 / 波次通道；本文件 §3 的"目标位置"即唯一归口；
- 不改 `maps/**` 的路径与塔位语义、不改 `assets/animations/**` 的动画数据格式；
- 不引入第三方 JSON 库（继续用 `util/json/MiniJson`）；
- 不改 `LivingEntity` 的公共契约（`MELEE_RANGE` 一致、死亡不碰列表金币）；
- 塔的"换类升级"架构（`ITowerUpgrade` + 原位替换）不推翻；
- 三档士兵"互不继承"的既有取舍不推翻（不把 3 级改成继承 2 级）；
- `GameConfig` / `GameController` / 抽象基类仅 A 可改，执行方交付 diff 由 A review 合入。
  > ⚠️ **2026-09-11 越界登记（P1-1 + P0-1/B-4 两个批次）**：为落实"溅射半径 / 敌人近战参数 JSON 化"，
  > 改了 `util.balance.BalanceTable`（A 侧数值数据类）——新增 ⑧ 组 3 字段与 `runtime()` 只读单例。
  > **未经 A 事前过手**，需 A 追认。
  > 若 A 不认可 `runtime()`（§九 要求唯一运行期入口 = `GameConfig`），回退方案：
  > ① 溅射半径 → 改走 `GameConfig` → 塔构造注入，需动 `Tower` 基类；
  > ② 敌人近战参数 → 走原定路径 `GameConfig` getter → `spawnEnemy` 显式传参（**通道已就绪，只差 A 接线**，
  >    且 `runtime()` 可随之删除）。**敌人实体类（`Enemy` 及 4 个子类）属 B 侧，改它们不算越界。**
  > 两个批次均**未动** `Main` / `GameController` / `GameConfig` / `Tower` / 任何接口。

---

## 12. 附：本次审计的证据索引

| 主题 | 关键位置 |
| :--- | :--- |
| 敌人攻击力写死 | `Enemy.java:33-34`、`FastEnemy.java:22-23`、`TankEnemy.java:22-23`、`BossEnemy.java:40-41` |
| 假配置通道 | `BalanceTable.java:60-61,151-174,201-211,274-279`、`BalanceEditorTool.java:327-355,383-394` |
| 波数覆盖 | `GameConfig.java:149-159`、`GameController.java:255-277,324-330`、`LevelWaves.java:82-99` |
| 溅射半径断链 | `CannonTower.java:37,53`、`EliteCannonTower.java:25`、`MasterCannonTower.java:27`、`Bomb.java:20,43` |
| 三级链断链 | `Main.java:89-91`、`GameController.java:457-461`、`TowerType.java:16-26` |
| 选中通知悬空 | `GameController.java:58,88`、`GameView.java:428-440`、`TowerDetailPanel.java:30,86-110` |
| Tower 死代码 | `Tower.java:23,25,73-78,93-94` + 7 处 `upgradeCostBase` 赋值 |
| Boss 技能 | `BossEnemy.java:21,24,40-41,44-68`、`GameController.java:136-139,160-178` |
| 减伤/破甲 | `Arrow.java:31-38`、`Bomb.java:40-50` |
| 资产缺口 | `AssetKey.java:11-23`、`assets/sizes.json:1-20`、`maps/default/waves.json:1-67` |
