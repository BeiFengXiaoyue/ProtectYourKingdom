# 友方士兵子系统说明

> 归属：友方单位开发
> 版本：v1.1（补充：命中飘字、打击动作接入、尺寸改由 `sizes.json` 驱动）
> 覆盖：1 级 `Soldier` / 2 级 `EliteSoldier` / 3 级 `RoyalSoldier`
> 本文档说明**三档士兵的数值、契约与使用方式**，供架构师 A / 防御塔 C / UI D / 素材 E 对照。
> 与《防御塔子系统说明》互补：那篇讲塔与升级链，本篇讲塔里"产出的兵"。

---

## 1. 三档士兵一览

| 等级 | 类 | 默认 HP | 速度 px/s | 攻击 | 冷却 | 索敌视野 | 体型 | 显示名 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 级 | `Soldier` | 50（由兵营传入，类内不写死） | 40 | 8 | 800ms | 90 | 32×32 | 士兵 |
| 2 级 | `EliteSoldier` | 100 | 65 | 14 | 700ms | 120 | 36×36 | 精英士兵 |
| 3 级 | `RoyalSoldier` | 150 | 72 | 20 | 620ms | 150 | 40×40 | 皇家卫士 |

- 三档数值**依次提高**（HP 50 → 100 → 150，速度 40 → 65 → 72，攻击 8 → 14 → 20，冷却 800 → 700 → 620ms，视野 90 → 120 → 150，体型 32 → 36 → 40）。
- 体型**不写在代码里**：由 `assets/sizes.json` 按类名驱动（见 §6），三档尺寸条目已登记。
- 1 级沿用现状：数值**由生产方注入**（`Barrack` 传 **50/40/8/800**），类内不写死；2/3 级额外提供**类内默认档案**（`DEFAULT_HP` 等 public 常量）作直接实例化时的兜底，同时保留全参构造供生产方注入——数值口径与 1 级一致。
  ⚠️ **现状**：`EliteBarrack` / `MasterBarrack` **并未**注入 100/65/14/700，只覆写了士兵数与 HP
  （`EliteBarrack` 3 兵 / 80 HP、`MasterBarrack` 4 兵 / 80 HP），速度/攻击/冷却仍继承 `Barrack` 的 40/8/800；
  且 `Barrack.update()` 恒 `new Soldier(...)` → **2/3 级兵营实际仍产 1 级兵**（见 §5.4 与 §8 待办）。
- 2 级档案（100/65/14/700）与《防御塔子系统说明》§14 的 `EliteBarrack` 参数**并不一致**：§14 只提高士兵数与 HP（3 / 80），伤害/间隔/速度仍 8/800ms/40。
  因此**当前若把 2 级兵营的产出换成本类，会改变战斗数值**——需先统一口径（改 §14 或改本表）。

---

## 2. 分级方式：三档并列，互不继承（重要）

三档士兵都是 `Ally` 的**直接子类**，彼此没有继承关系：

```
Ally（抽象：索敌策略 + 对敌碰撞反应）
├── Soldier        1 级
├── EliteSoldier   2 级   ← 不继承 Soldier
└── RoyalSoldier   3 级   ← 不继承 EliteSoldier
```

**为什么这么分**：每档的数值、视野、体型、渲染各自独立，改 2 级不会波及 1/3 级；
代价是索敌/追击/回防三段逻辑**每档各写一份**，需要统一调整时要三处同改——这是刻意的取舍，
按项目"实体类自包含"的既有口径（`Soldier` 本身就是自包含的）可接受。

> 与塔的升级链不同：塔是"**原位删旧塔、放新塔**"的换类升级（`ITowerUpgrade` 给出下一级），
> 士兵三档没有链式接口，它们是三个可独立生产的对象；"升到 2 级"在士兵侧的含义是
> **生产方改为产出 2 级兵**（见 §5.4），不是把 1 级兵原地升级。

---

## 3. 共同契约（继承 `Ally` / `LivingEntity`）

| 项 | 约定 |
| :--- | :--- |
| 必实现 | `findTarget(List<Enemy>)`（`Ally` 的抽象方法） |
| 近战三件套 | `attackDamage / attackCooldown / maxAttackCooldown` 与 `tryAttack()` 由 `LivingEntity` 提供，构造期经 `Ally(x, y, hp, speed, atk, cd)` 注入 |
| 碰撞拦截 | `Ally.handleEntityCollision()` 已实现：碰到敌人即锁定 `target` 并近战反击，三档共用，**子类无需重写** |
| 回防锚点 | 出生点 `(homeX, homeY)` 由各级在构造期自行记录，`target` 为空/死亡时走回待命 |
| 贴身距离 | `MELEE_RANGE = 10` 必须与 `Enemy.chaseAndAttack` 的贴身阈值一致，**三档都不得单独调**（调了会出现"追不上/抖在敌人身上"） |
| 帧步进 | 移动按 `speed * 0.016`（60fps 定步长），与敌人一致；`LivingEntity.update()` 每帧固定扣 16ms 冷却 |
| 死亡 | `onDeath()` 只做**表现**（粒子爆散）；音效由基类 `takeDamage()` 触发；**移除与结算归 `GameController`**，实体不碰列表/金币 |
| 命中飘字 | `Ally.tryAttack()` 覆写：命中判定沿用基类，命中后调 `Ally.onHit(target)`——与 `Bomb.onHit` 同一套 fxText 写法（目标头顶 y-24、白色、`-伤害`）。三档共用，敌人不受影响；近战**只做飘字**，不加粒子/震屏/命中音 |
| 叠加后钩子 | `Ally.renderPostAnim(gc)` 默认空；精英兵种覆写它画等级标记（金环/盔缨），由渲染层在动画帧叠加之后调用 |

---

## 4. 索敌与战斗流程（现状口径）

士兵拿目标有**两条通路**，两条都已接线：

| 通路 | 状态 | 说明 |
| :--- | :--- | :--- |
| ① 碰撞拦截 | ✅ 已接线 | `GameController.processEntityCollisions()` 每帧对"敌人 × 友方"做圆碰撞 → `Enemy.onCollision` / `Ally.onCollision` → 双方互锁并近战（敌人进 `isEngaged` 追击态，停止沿路径前进） |
| ② 主动索敌 | ✅ 已接线 | `GameController` 友方循环：`a.engage(a.findTarget(enemies))` → 视野内最近敌人写进 `target`，随后 `a.update()` 驱动移动（`Ally.engage` 只在无目标/目标已死时接手，防逐帧换目标抖动） |

> **接线的意义**：士兵出生在兵营（塔位）处，而塔位离路径中线 60~97px（`maps/default/spots.json`
> 实测），碰撞触发要求距离 < 双方半径和（1 级士兵 16 + 普通敌人 24 = 40px）——**只靠碰撞的话
> 敌人永远撞不到士兵，士兵会一直"站桩"不动**。有了主动索敌，士兵才会主动出击视野内敌人、
> 目标死亡后走回出生点待命。
>
> ⚠ 视野 90px 覆盖不到最远的塔位（#6 号塔位距路径 97.4px），建在那里的兵营产出的士兵
> 仍会站着不动——要覆盖全部塔位需把 `Soldier.VISION_RANGE` 提到 ~110，属改数值，另议。

---

## 5. 使用方式

### 5.1 直接实例化（走类内默认档案）

```java
Ally a = new EliteSoldier(x, y);   // 2 级：100 / 65 / 14 / 700
Ally b = new RoyalSoldier(x, y);   // 3 级：150 / 72 / 20 / 620
```

### 5.2 生产方注入数值（推荐，与 1 级口径一致）

```java
Ally s = new EliteSoldier(x, y, hp, speed, attackDamage, attackCooldown);
```

全参构造签名与 `Soldier` 完全一致，便于把兵营的生产对象**成对替换**。

### 5.3 加入战场：只走 `GameController.addAlly(Ally)`

```java
controller.addAlly(new RoyalSoldier(x, y));   // 注入事件通道 + 进 allies 注册表 + 出兵音效
```

不要在别处自己往 `allies` 列表里塞——否则漏掉 `attachFx`（音效/粒子/飘字全哑）与出兵音效。

### 5.4 让兵营产出高级士兵（当前未接线，改动归 C/A）

`Barrack.update()` 现在写死 `new Soldier(...)`。要让 2/3 级兵营产出精英兵，推荐做法是
在 `Barrack` 抽一个工厂钩子、由 `EliteBarrack` 覆写：

```java
// Barrack
protected Ally createSoldier() {
    return new Soldier(x, y, soldierHp, soldierSpeed, soldierAttack, soldierCooldown);
}
// EliteBarrack
@Override protected Ally createSoldier() {
    return new EliteSoldier(x, y, soldierHp, soldierSpeed, soldierAttack, soldierCooldown);
}
```

> ⚠ 两条注意：
> 1. **必须在兵营侧生产**（而不是监听 `allySink` 事后替换），否则兵营自己的"上限 3/5"计数
>    与实际战场上的兵对不上；
> 2. 本文档**不改** `Barrack` / `EliteBarrack`——抽取钩子属塔负责人 C 的改动范围，
>    由 A/C 决定后再动。

### 5.5 调参位置（当前口径）

| 数值 | 位置 |
| :--- | :--- |
| 1 级士兵数值 | `Barrack` / `EliteBarrack` 的实例字段（`soldierHp` 等） |
| 2/3 级默认档案 | `EliteSoldier.DEFAULT_*` / `RoyalSoldier.DEFAULT_*`（public 常量） |
| 生产方注入值 | 构造期参数，最高优先级 |

> 后续若按《数值配置 JSON 化规范》二期把"每类单位数值"搬进 `config/balance.json`，
> 以该规范为准统一改口径，本文档 §1 表格届时同步更新。

---

## 6. 素材与动画接入

| 项 | 契约 |
| :--- | :--- |
| 贴图 key | `AssetKey.ELITE_SOLDIER` → `assets/elite_soldier.png`；`AssetKey.ROYAL_SOLDIER` → `assets/royal_soldier.png`（**缺图自动回退色块，不报错**，无需等素材） |
| 行为动画 | 三档**共用** `/assets/animations/allies/soldier.json`（精英兵暂无专属帧），由 `view/Main.java` 登记；近战时自动切 `attack` 模式播 10 帧挥砍（判定见 `UnitPoseResolver`）。⚠️ **现状：`Main` 只登记了 `Soldier`**，`EliteSoldier` / `RoyalSoldier` 尚未登记（复用同一 JSON 即可，待补两行） |
| 打击动作 | 素材已就位：`assets/allies/soldier/attack_0~9.png`，JSON 里 `attack` 已引用。精英兵复用同一套帧，等级差异由**体型 + 标记**体现 |
| 等级标记 | 动画帧会画满整个身体框，故标记改由 `Ally.renderPostAnim` 在帧之上补画（2 级金环 + 肩章星；3 级加粗金环 + 星 + 红盔缨）；`render()` 里的色块回退保持原样 |
| 命中飘字 | 由 `Ally.tryAttack` 触发 `fxText.showFloatingText(目标 x, y-24, "-伤害", "WHITE")`；飘字层实现见 `GameView.showFloatingText`（寿命 45 帧、上浮 + 淡出、黑描边） |
| 尺寸 | 由 `assets/sizes.json` 按类名驱动（`Soldier` 32 / `EliteSoldier` 36 / `RoyalSoldier` 40），类内**不写死** |

**无素材时的回退外观**（当前实际显示）：

| 类别 | 外观 |
| :--- | :--- |
| 1 级 | 蓝圆身 `#3498db` + 深蓝边 `#1f618d` |
| 2 级 | 蓝圆身 + **金色精英环** `#8a6d1f` + 肩章金星 `#ffd75e` |
| 3 级 | **深蓝**圆身 `#2c6fbb` + **加粗**金环 `#b8860b` + 肩章金星 + **红色盔缨** `#e74c3c` |

> 抗眩晕/受击闪烁等表现三档一致；血条宽度随体型自动变化（32/36/40 宽）。
> ⚠️ 现状：`Main` 只登记了 1 级 `Soldier` 的行为动画，**2/3 级尚未登记**（补齐后即播放帧动画）；上面这张表是无贴图/无帧时的兜底层；
> 等级标记由 `renderPostAnim` 画在动画帧之上，所以动画播放时标记依然可见。

---

## 7. 新增一档士兵的扩展流程

1. 在 `model/ally/` 新建 `XxxSoldier extends Ally`（**不要**继承 `Soldier`/`EliteSoldier`，保持三档并列）；
2. 照抄三件套骨架：`VISION_RANGE` / `MELEE_RANGE(=10)` / `RETURN_EPSILON` + `homeX/homeY`
   + `findTarget` / `move` / `returnHome` / `onDeath` / `render`；
3. 追加贴图 key（`AssetKey`）与 `render` 回退色块；需要在动画帧之上保留等级标记就覆写 `renderPostAnim`；
4. 在 `assets/sizes.json` 的 `entities` 里加一条 `"XxxSoldier": {"width":W,"height":H}`（**尺寸不写进代码**）；
5. 由生产方构造 → `GameController.addAlly(...)` 上战场；
6. 需要动画再补 `assets/animations/allies/xxx_soldier.json` 并在 `Main` 登记（暂无专属帧时可先指向 `soldier.json` 复用）。

---

## 8. 待办 / 验收

- [x] 新建 `EliteSoldier`（2 级）、`RoyalSoldier`（3 级），均**直接继承 `Ally`**、互不继承；
- [x] 三档数值依次提高（§1 表格）；
- [x] `AssetKey` 补 `ELITE_SOLDIER` / `ROYAL_SOLDIER` 两个 key；
- [ ] **谁产 2/3 级兵**：`Barrack` 抽 `createSoldier()` 钩子 + `EliteBarrack` 覆写（归 C/A，见 §5.4）；
- [x] **主动索敌接线**：`GameController` 每帧 `engage(findTarget(...))` 写入 `target`（已接线，见 §4）；
- [~] **打击动作接入**：三档共用 `soldier.json` 的 `attack` 十帧；`Main` **仅登记了 `Soldier`**，`EliteSoldier` / `RoyalSoldier` 的登记待补；
- [x] **命中飘字**：`Ally.tryAttack` 覆写 + `GameView.showFloatingText` 实现（含颜色名与 `#RRGGBB` 解析）；
- [x] **尺寸对齐**：`sizes.json` 补 `EliteSoldier` 36 / `RoyalSoldier` 40，两个类删掉写死的宽高；
- [ ] **命中音效**：近战命中音素材缺失（现仅有挥砍声 `melee_attack.wav`），后续交音效负责人；接入点建议在 `SoundManager.onUnitAttack`；
- [ ] 贴图 `elite_soldier.png` / `royal_soldier.png`（归 E，**当前缺失、回退色块**；也可改用专属动画帧替代贴图）；
- [ ] 专属动画 JSON（归 D/E）：做好后把 `Main` 里的登记从 `soldier.json` 换成各自的 JSON；
- [ ] 验收：`addAlly` 后三档士兵均能被敌人拦截并近战；命中时目标头顶冒 `-伤害` 白色飘字；
      贴身时播 `attack` 挥砍；精英兵动画帧之上仍见金环/盔缨；实测数值与 §1 一致；缺图时回退色块不报错。

---

## 9. 范围红线

- **不改 1 级 `Soldier` 的现有行为与数值口径**（它仍是 `Barrack` 生产的那一个，且三级不靠它派生）；
- **不改 `Barrack` / `EliteBarrack` 的生产逻辑**：本文档只说明"要接怎么接"，改动归 C/A；
- **三档互不继承是刻意选择**：不要为"省代码"把 3 级改成继承 2 级，也不要给 `Soldier` 加
  供精英档覆写的钩子——一旦加钩子，三档又会耦合回继承链；
- `Ally` 上现有两个**表现类**钩子（`tryAttack` 命中飘字、`renderPostAnim` 等级标记），
  它们只影响"看起来怎样"，**不得往里面塞战斗逻辑**（数值/索敌/移动仍各档自持）；
- 不改 `LivingEntity` 的公共契约（`MELEE_RANGE` 一致、死亡不碰列表金币等）。

---

## 附：一句话选型

| 场景 | 用哪个类 |
| :--- | :--- |
| 常规拦路兵（1 级兵营） | `Soldier`（`Barrack` 自动生产） |
| 中期更强拦路兵（2 级兵营 / 精英档） | `EliteSoldier` |
| 后期最高档拦路兵 | `RoyalSoldier` |
