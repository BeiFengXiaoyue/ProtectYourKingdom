# 今天干啥（内部版）

> 就是给咱们六个人分活儿用的，别当正式文档看。
> 只讲"今天谁做什么"，来龙去脉在《整改分工方案-v1.0》和《整改方案-文档与代码一致性》里。
> 日期：**2026-09-11**

---

## A（朱道玺）—— 集成/架构

- 你是**唯一能碰** `Main` / `GameController` / `GameConfig` / 抽象基类 / 接口 的人。别人要改这些，都是提需求给你，**你来落文件**。
- 等 B 把 4 个敌人构造扩参（B-4）做完，你去 `spawnEnemy` 把攻击力/冷却**这 3 处传参补上**，5-1 才算真正通。
- 兵营升级/出售后要**清掉旧兵营的士兵**（现在没清，旧兵会一直留在场上）：配合 C 的兵营出口，在 `upgradeTower` / `sellTower` 里把旧士兵从 `allies` 移除。（P1-9，新发现的 bug）
- 每个人交上来的 diff，你过一遍再合。
- 每搞完一批，跑一遍 javac 编译验证（82 个源文件，**0 报错**为准）。

## B（邝展颖）—— 实体

> 今天你活儿最多，加油。

1. ~~`Bomb` 构造加个 `splashRadius` 参数，把里面**写死的 60 去掉**。~~（B-1）✅ **已完成 2026-09-11**：
   `Bomb` 构造扩参，`SPLASH_RADIUS = 60` 已删；半径改由 `config/balance.json` 的 3 个键驱动
   （`cannonSplashRadius`/`eliteCannonSplashRadius`/`masterCannonSplashRadius`，默认 50/65/80）。
   ⚠️ 顺带改了 `BalanceTable`（原属 A 侧），并被 C-2 一并做掉了 → **需 A 追认后合入**。
2. Boss 震地：加"**狂暴后每 15 秒**"的计时；狂暴时**攻击间隔再砍 30%**。（B-2）
3. ~~4 个敌人（普通/快速/重甲/Boss）构造**扩参**，加上攻击力和攻击冷却；**老签名留着做个重载**，别把别处调用搞崩。~~（B-4）
   ✅ **已完成 2026-09-11**：4 个敌人都已扩参为 `(x, y, hp, speed, goldReward, attackDamage, attackCooldownMs)`，
   **老 5 参签名保留为重载**（不破坏现有 `spawnEnemy` 调用）。老签名改为从 `config/balance.json` 取
   （`{normal|fast|tank|boss}AttackDamage` / `…AttackCooldownMs`，默认 5/1000、4/700、9/1400、22/1100），
   **实体类内写死的攻击数值已全部删除**（含基类 `Enemy` 那对 5/1000）。→ **JSON 调试通道已可用**，改 JSON 重启即生效。
   ⏳ **仍待 A（5-1）**：`GameController.spawnEnemy` 改用新签名、把 `config.getXxxAttackDamage()` 显式传进去。
   **A 不改也能跑**（老签名已从 JSON 取值），但 5-1 不落地则新签名无调用方。
4. 重甲减伤：`Enemy` 受击按 `tankPhysicalReduction` 算；`Arrow.onHit` 走减伤、`Bomb.onHit` 无视（破甲）；**飘字改成显示实际伤害**。（B-5）
5. 士兵**切不到 idle**：现在锁了目标就一直走/打，待命动画进不去。改成"目标跑出视野（90px）就撒手"，让士兵回岗站定。（P1-8，新发现的 bug）

- ⚠️ 召唤那事儿（B-3）**已经拍板不做了，别做**。

## C（唐新雄）—— 防御塔

1. 3 个精英塔的 `render` 改用新的 `AssetKey`。（C-1，等 E-1 的 key 加好）
2. ~~`CannonTower.attack` 生成 `Bomb` 时，把 `splashRadius` 传进去。~~（C-2，跟 B-1 配套）✅ **已完成 2026-09-11**
   （与 B-1 同批交付）：`CannonTower.attack` 已传半径；三个炮塔改为**构造期从 `balance.json` 取值**，
   原先写死的 `= 50/65/80` 已删。改半径请改 JSON，不用动代码。
3. `Barrack` 抽个 `createSoldier()` 钩子，`EliteBarrack` 产 `EliteSoldier`、`MasterBarrack` 产 `RoyalSoldier`。（C-3）
   - ⚠️ **只换"产哪个兵"，士兵数值一个都别动**，以 F 给的数值文档为准。
4. 兵营加个"交出自己产的兵"的方法（`getSoldiers()` / `releaseSoldiers()`），好让 A 在升级/出售时清掉旧兵。（P1-9，新发现的 bug）

## D（张峪嘉）—— 界面

1. `GameView` 的 `showBossWarning()` 实现一下：红字横幅"BOSS 来袭"停几秒，配上闪屏/震屏，照现有那套**时间驱动的特效**写法来。（D-1）
2. 确认下 1-1（三个塔改 `render`）算 C 的、1-3（`Main` 登记）算 A 的，别到时候扯皮。（D-3）

## E（李贤宇）—— 素材与音效

1. `AssetKey` 里加 3 个精英塔 key：`ELITE_ARROW_TOWER` / `ELITE_CANNON_TOWER` / `ELITE_BARRACK`，C-1 要用。（E-1）
2. 精英兵动画的帧图**确认下能不能用**，给 A 那边登记兜底。（E-3）

## F（张少睿）—— 数值 + 验收

1. `balance.json` 里把 **8 个键**填上：4 个敌人的攻击力 + 攻击冷却（默认 5/1000、4/700、9/1400、22/1100）；顺手把 `BalanceEditorTool` 的输入框和 `FIXED_KEYS` 补上。（F-1）
   - ⚠️ 编辑器下次保存，会把 A 新加的 9 个键**按默认值写进 JSON**，正好一并核对。
2. `tankPhysicalReduction`（0.3）等减伤/破甲的数值定下来。（F-2）
3. 最终**验收还是你统管**，各批做完按《整改方案》§10 逐项过。（F-4）
4. 波次数据按需要维护，记住是**测试用例，别写死波数**。（F-6）

---

## 今天谁卡谁

```
A 数值通道已铺好 ──┬─▶ F 填 8 个键（F-1）──┐
                    │                        ├─▶ F 验收
                    └─▶ B 实体扩参（B-4）────┘
B-4 做完 ──▶ 喊 A 补 spawnEnemy 传参 ──▶ 5-1 才算通
E-1 加好 key ──▶ C-1 才能改 render
B-1（Bomb 扩参）↔ C-2（塔传半径）是**一对**，谁先动都行，最后要能对上   ✅ 已同批完成（2026-09-11），不再卡人
C 的兵营出口（C-4）──▶ A 在 upgradeTower / sellTower 清旧兵（P1-9）
B 的士兵撒手（B-5）独立，不卡别人
```

- F-1 和 B-4 **不互相等**，可以并行开工。
- ✅ **2026-09-11 进展**：**B-4 已完成**（4 敌人扩参 + 老签名保留）；**F-1 的 JSON 取值部分已一并完成**
  （8 个攻击/冷却键已写进 `balance.json`，老签名直接读 JSON）——B-4 与 F-1 不再卡任何人。
  ⏳ **仅剩 5-1**：`GameController.spawnEnemy` 改用新签名显式传参，**归 A**；
  不落地也不影响运行（老签名已从 JSON 取值），只是新签名暂无调用方。
  ⏳ **F-1 剩余**：`BalanceEditorTool` 输入框补 8 个攻击键 + 3 个溅射键（否则工具保存会按默认值写回）。

---

## 几个提醒

- `Main` / `GameController` / `GameConfig` / 抽象基类 / 接口，**只有 A 能动**。
  > ⚠️ **2026-09-11 例外登记**：B-1/C-2 同批交付时改了 `util.balance.BalanceTable`（数值数据类，原属 A）
  > ——新增 ⑧ 组 3 字段 + `runtime()` 只读单例。**未经 A 事前过手，属越界，需 A 追认**；
  > 若 A 不认可 `runtime()` 这条路（§九 要求「唯一运行期入口 = `GameConfig`」），
  > 回退方案是把半径改走 `GameConfig` → 塔构造注入，届时需动 `Tower` 基类。
  > 本次**未动** `Main` / `GameController` / `GameConfig` / `Tower` / 任何接口。
- **不引第三方 JSON 库**，继续用 `MiniJson`。
- 塔的"换类升级"架构不动；三档士兵**互不继承**这个取舍也不动。
- 波次数据是测试用的，不写死波数。
