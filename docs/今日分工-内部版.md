# 今天干啥（内部版）

> 就是给咱们六个人分活儿用的，别当正式文档看。
> **下午更新（2026-09-11）**：B / C / D 三条分支已合入 `dev`，下面**只列还没做完的**。
> 来龙去脉在《整改分工方案-v1.0》和《整改方案-文档与代码一致性》里。

---

## A（朱道玺）—— 集成/架构

> **2026-09-11 收尾：以下三项已全部完成**（本次改动，dev 分支）。

- **`spawnEnemy` 补传参（5-1）✅ 已完成**：4 个 switch 分支改用 7 参构造，把 `GameConfig` 的
  8 个攻/冷 getter 显式注入 → 5-1 闭环。
- **P1-9 接线 ✅ 已完成**：新增 `GameController.retireBarrackSoldiers(Tower)`（调 `Barrack.releaseSoldiers()`
  + `allies.removeAll(...)`），在 `upgradeTower`（替换前）/ `sellTower`（出售后）各调一次。
  ⚠️ 此前的 bug（升级后新旧两批兵并存、出售后产生孤儿兵）已消除。
- **P1-4 拍板 ✅ 已定**：维持"**继续放一放**"（`GameView` 直连 `TowerDetailPanel`，功能可用，不动代码）。
- **越界追认 ✅ 已追认**：接受现状、不回退 —— `BalanceTable.runtime()` 只读单例保留；
  `LivingEntity` 减伤字段 + `takeDamage` 返回 `int`（`Arrow`/`Bomb` 已消费，确认无需再改）。
- **顺手清理 ✅**：`GameController.settleBossStompRequests()` 过期 javadoc（"仅半血狂暴触发一次"）已改写为周期性描述。
- 收各人 diff 合入；每批 javac 验证（82 文件 0 报错）。

## B（邝展颖）—— 实体

- 只剩 **P1-8（士兵不待命 `idle`）**：在 `Soldier` 侧，目标**出视野（>90px）就撒手**（`target = null`）→ `returnHome()` 回岗站定，才能进 idle。改 `Soldier`，**不碰抽象基类 `Ally`**。
- 顺带：`UnitAnimator.hasOverlay()` 把 `moved` **写死成 true**（`UnitAnimator.java:82`），与"是否真位移"不自洽，一并按实际位移解析。
- 一个口径待确认：近战（士兵打重甲）现在**也吃减伤**；文档原话"仅普通箭矢吃减伤"若仍作数，需另改。

## C（唐新雄）—— 防御塔

- **C-1 ✅ 已完成（2026-09-11）**：6 个精英/大师塔的 `render` 已换成各自专属 key
  （`ELITE_*` / `MASTER_*`），精英/大师塔不再与 1 级塔同外观。
- ⛔ **注意政策变更**：塔数值 **不再要求 JSON 化**（直接写死）→ 你之前提的 `TowerParams` 重构
  （`feature/tower` 分支）**不再需要，不采用**。详见《整改方案》§9 分支弃用登记。

## D（张峪嘉）—— 界面

- 今天的活已清。有余力就**复验 Boss 预警**：进最终波看红字"BOSS 来袭"横幅 + 闪屏/震屏是否正常、非最终波不触发。

## E（李贤宇）—— 素材与音效

- **E-1 ✅ 已完成（2026-09-11，`e326487`）**：`AssetKey` 已补齐 **6 个**精英/大师塔 key
  （`ELITE_*` / `MASTER_*`）+ 对应贴图，C-1 已随之完成。
- E-3：四种敌人的行为动画帧图已入库（含 boss 37 帧），扫一眼动画表要不要再调。

## F（张少睿）—— 数值 + 验收

- **F-1 剩余**：`balance.json` 那 8 个键 B 已写入并生效，还差 **`BalanceEditorTool` 的输入框 + `FIXED_KEYS`**。
  ⚠️ 不加的话，下次用编辑器保存会把新键**按内置默认值写回**。
- **F-4 验收**：按《整改方案》§10 逐项过；其中**第 4（减伤/破甲）、7（溅射半径）、8（Boss 震地/狂暴）条待进游戏目视复验**（通道已自验，缺实机观察）。

---

## 今天谁卡谁

```
E-1 → C-1  ✅ 已双双完成（2026-09-11），不再卡人
A 的 spawnEnemy 传参 ✅ 已完成（5-1 闭环）
A 的 P1-9 接线 ✅ 已完成（升级/出售不再留旧兵）
P1-8（B）独立，不卡别人
```

---

## 几个提醒

- `Main` / `GameController` / `GameConfig` / 抽象基类 / 接口，**只有 A 能动**（本轮 B/C 的越界改动已在上面登记待追认）。
- **不引第三方 JSON 库**，继续用 `MiniJson`。
- 塔的"换类升级"架构不动；三档士兵**互不继承**这个取舍也不动。
- 波次数据是测试用的，不写死波数。
