# 今天干啥（内部版）

> 就是给咱们六个人分活儿用的，别当正式文档看。
> **下午更新（2026-09-11）**：B / C / D 三条分支已合入 `dev`，下面**只列还没做完的**。
> 来龙去脉在《整改分工方案-v1.0》和《整改方案-文档与代码一致性》里。

---

## A（朱道玺）—— 集成/架构

- **`spawnEnemy` 补传参（5-1）**：B-4 已落地（4 个敌人构造扩参为 7 参、老 5 参重载保留），你去 `spawnEnemy` 按新签名把攻击力/冷却**传上**，5-1 才算真正闭环。
- **P1-9 接线**：C 已在 `Barrack` 给出出口 `getSoldiers()` / `releaseSoldiers()`（**已就绪**）。请在 `upgradeTower` / `sellTower` 里调用它，把旧兵从 `allies` 移除。
  ⚠️ **不调用，升级后旧兵照样留着，这个 bug 就还在。**
- **P1-4 拍板**：`ITowerSelectionNotifier` 走 **(a) 补线** 还是 **(b) 删悬空契约**，定一个（现在 `setSelectionNotifier` 仍无人调用）。
- **越界追认**：B/C 这轮动了 `util.balance.BalanceTable`（加 `runtime()` 只读单例）与 `LivingEntity`（加 `damageTakenMultiplier`、`takeDamage` 返回类型 `void→int`）——都在 A 的红线内，**需你追认**；不接受有回退方案（见《整改方案》§11）。
- 收各人 diff 合入；每批 javac 验证（82 文件 0 报错）。

## B（邝展颖）—— 实体

- 只剩 **P1-8（士兵不待命 `idle`）**：在 `Soldier` 侧，目标**出视野（>90px）就撒手**（`target = null`）→ `returnHome()` 回岗站定，才能进 idle。改 `Soldier`，**不碰抽象基类 `Ally`**。
- 顺带：`UnitAnimator.hasOverlay()` 把 `moved` **写死成 true**（`UnitAnimator.java:82`），与"是否真位移"不自洽，一并按实际位移解析。
- 一个口径待确认：近战（士兵打重甲）现在**也吃减伤**；文档原话"仅普通箭矢吃减伤"若仍作数，需另改。

## C（唐新雄）—— 防御塔

- 只剩 **C-1（三个精英塔 `render` 换新 `AssetKey`）**：现在 `EliteArrowTower` / `EliteCannonTower` / `EliteBarrack` 还在用基础塔的 key，**等 E-1 的 key**。

## D（张峪嘉）—— 界面

- 今天的活已清。有余力就**复验 Boss 预警**：进最终波看红字"BOSS 来袭"横幅 + 闪屏/震屏是否正常、非最终波不触发。

## E（李贤宇）—— 素材与音效

- 只剩 **E-1（`AssetKey` 加 3 个精英塔 key）**：`ELITE_ARROW_TOWER` / `ELITE_CANNON_TOWER` / `ELITE_BARRACK`。
  ⚠️ **C-1 卡在这，先出这个。**
- E-3：四种敌人的行为动画帧图已入库（含 boss 37 帧），扫一眼动画表要不要再调。

## F（张少睿）—— 数值 + 验收

- **F-1 剩余**：`balance.json` 那 8 个键 B 已写入并生效，还差 **`BalanceEditorTool` 的输入框 + `FIXED_KEYS`**。
  ⚠️ 不加的话，下次用编辑器保存会把新键**按内置默认值写回**。
- **F-4 验收**：按《整改方案》§10 逐项过；其中**第 4（减伤/破甲）、7（溅射半径）、8（Boss 震地/狂暴）条待进游戏目视复验**（通道已自验，缺实机观察）。

---

## 今天谁卡谁

```
E-1 加好 key ──▶ C-1 才能改 render        ← 唯一还卡着的依赖
A 的 spawnEnemy 传参 ── 依赖的 B-4 已落地，可直接开工
A 的 P1-9 接线 ── 依赖的 C 出口已就绪，可直接开工
P1-8（B）独立，不卡别人
```

---

## 几个提醒

- `Main` / `GameController` / `GameConfig` / 抽象基类 / 接口，**只有 A 能动**（本轮 B/C 的越界改动已在上面登记待追认）。
- **不引第三方 JSON 库**，继续用 `MiniJson`。
- 塔的"换类升级"架构不动；三档士兵**互不继承**这个取舍也不动。
- 波次数据是测试用的，不写死波数。
