# 文档与代码一致性整改 · 分工方案

| 项 | 内容 |
| :--- | :--- |
| 文档名称 | 文档与代码一致性整改 · 分工方案 |
| 版本 | **v1.0** |
| 编制日期 | **2026-09-10** |
| 状态 | 待评审 |
| 适用分支 | dev（feature） |
| 关联文档 | `docs/整改方案-文档与代码一致性.md`（问题描述与整改动作的**唯一出处**；本文档**只做分工**，不重复描述问题） |
| 上游依据 | 《计划书 v3.0》分工代号、`docs/接口契约与抽象类说明.md` §1.1 交付红线 |

## 修订记录

| 版本 | 日期 | 修订内容 |
| :--- | :--- | :--- |
| v1.0 | 2026-09-10 | 初版：把《整改方案》剩余批次拆分至六人，每项区分「主责 / 落文件方」两栏 |

---

## 0. 本文档定位与两条红线

| 项 | 说明 |
| :--- | :--- |
| 定位 | 把《整改方案》§7/§8 的整改项**落到人**，输出可直接分发的按角色任务清单（§3） |
| 红线 1 | `Main` / `GameController` / `GameConfig` / 抽象基类 / 接口 **仅 A 可改** → 凡落这些文件的任务，主责方**提需求、A 落文件** |
| 红线 2 | 每项必须区分 **主责（谁设计/提测）** 与 **落文件方（谁改代码）**，避免出现"D 要改 `Main`"式死结 |
| 状态图例 | ✅ 已完成 ｜ 🟡 部分完成（本侧已落，卡对侧依赖） ｜ ⬜ 待做 ｜ ⏸ 暂缓（已裁定不做） ｜ ❓ 待定（需拍板后才能排期） |

**已完成的整改项（不在本方案排期）**：`Tower` 死代码删除与 `level` 改类身份（含文档联动）、`BalanceTable` 未知键告警、`BalanceEditorTool` 自定义变量提示、10 份文档对齐 + `README`。

**A 侧本轮已完成（2026-09-10，第二批）**：

- `Main` 登记 `EliteSoldier` / `RoyalSoldier` 动画（1-3）；
- `Main` 登记 3 个 `*_MASTER` 升级工厂（2-1，L2→L3 打通；L3 尺寸待 E-2）；
- `GameController` 轮询 `consumeStompRequest()` → 全塔 `stun(3000)`（3-2 的 A 侧）；
- `BalanceTable` / `GameConfig` 新增 9 键通道（5-1 的 8 个近战键 + 5-2 的 `tankPhysicalReduction`；**`spawnEnemy` 注入待 B-4**）。

---

## 1. 六人角色与职责域

> 代号来源：《接口契约与抽象类说明》§1.1（A＝朱道玺、B＝邝展颖、C＝唐新雄、D＝张峪嘉、E＝李贤宇、F＝张少睿）。

| 代号 | 姓名 | 职责域（可改的代码） | 本方案任务量 |
| :--- | :--- | :--- | :--- |
| **A** | 朱道玺 | 抽象基类、controller 接口、`GameController`、`WaveManager`、`GameConfig`、`util/balance/*`、`view/Main`、公共契约文档 | 11 项（3 项已完成） |
| **B** | 邝展颖 | `model/enemy/*`、`model/ally/*`、`model/projectile/*`（含 `onHit`） | 5 项 |
| **C** | 唐新雄 | `model/tower/*`（9 个塔类 + 2 个能力接口） | 4 项 |
| **D** | 张峪嘉 | `view/GameView`、`view/HUD`、`view/TowerDetailPanel`、`TowerBuildMenu`、`util/editor/*` 可视化工具 | 3 项 |
| **E** | 李贤宇 | `model/AssetKey`、`resources/assets/*`（贴图/音效/尺寸 JSON） | 3 项 |
| **F** | 张少睿 | 验收；**数值文件与数值工具**（`config/balance.json`、`BalanceEditorTool`）；策划文档；波次数据维护 | 6 项 |

---

## 2. 分工总表（核心）

### 批次 1 · 资产与收尾（零依赖，先做）

| 编号 | 任务 | 主责 | 落文件方 | 交付物 | 依赖 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **1-1** | 激活 3 张精英塔死图 | E | **E**（`AssetKey`）+ **C**（3 个塔类 `render`） | `AssetKey` 增 `ELITE_ARROW_TOWER` / `ELITE_CANNON_TOWER` / `ELITE_BARRACK`；`EliteArrowTower` / `EliteBarrack` / `EliteCannonTower` 的 `render` 改用对应 key（`Master*` 无专属图，复用 ELITE key） | 无 | ✅ **已完成（2026-09-11）**：E-1 实补 **6 个** key（3 `ELITE_*` + 3 `MASTER_*`，`e326487`）；C-1 随之把 6 个精英/大师塔 `render` 换成各自专属 key（`907bd45`） |
| **1-2** | 补 3 个 Master 塔尺寸 | E | **E** | `assets/sizes.json` 增 `MasterArrowTower` / `MasterCannonTower` / `MasterBarrack`（建议 48×48） | 无 | ✅ 已落（2026-09-11，`sizes.json:15-17` 三条 48×48） |
| **1-3** | 精英兵动画登记 | E | **A**（`Main` 唯一可改） | `Main` 补 2 行 `registerUnitAnimation("allies","EliteSoldier"/"RoyalSoldier","/assets/animations/allies/soldier.json")`；E 负责确认帧图可用 | `soldier.json` 已存在 | ✅ A 侧已落（2026-09-10；帧图已核对齐备，E-3 待 E 正式确认） |
| **1-4** | 清理残留文件 | A | **A** | 删 `src/main/resources/占位.txt`、`src/test/java/占位.txt`、根目录 `nul`（垃圾文件） | 无 | ✅ 已删（3 个文件，2026-09-10） |

### 批次 2 · 三级链打通 + Boss 预警

| 编号 | 任务 | 主责 | 落文件方 | 交付物 | 依赖 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **2-1** | L2→L3 升级打通 | **D**（交互提出需求） | **A**（`Main`） | `Main` 补 3 行 `registerUpgradeFactory(ARROW_MASTER / CANNON_MASTER / BARRACK_MASTER, …)` | **必须先做 1-2**（否则三级塔回退 20×20） | ✅ **已完全打通（2026-09-11）**：工厂已登记（A 侧 2026-09-10）+ 2-1 依赖的尺寸已补 → L3 不再回退 20×20 |
| **2-2** | `showBossWarning()` 实现 | D | **D**（`GameView:727`） | 红字横幅"BOSS 来袭"数秒 + `flashScreen`/`shakeScreen`，复用现有时间驱动特效写法 | 无 | ✅ **已完成（2026-09-11，D-1）**：`GameView.showBossWarning()` + 时间驱动横幅（"BOSS 来袭"）、闪屏/震屏，过期自清 |

### 批次 3 · 战斗机制补全

| 编号 | 任务 | 主责 | 落文件方 | 交付物 | 依赖 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **3-1** | 溅射半径传递 | B | **B**（`Bomb`）+ **C**（`CannonTower.attack`） | `Bomb` 构造扩参 `double splashRadius`（字段化，删 `SPLASH_RADIUS = 60`）；`CannonTower.attack` 传 `splashRadius`；`Elite*` / `Master*` 已覆写字段自动生效 | 无 | ✅ **已完成（2026-09-11）**，并**追加** JSON 化：3 档半径改由 `balance.json` ⑧ 组 3 键驱动，塔类写死的 `= 50/65/80` 已删。⚠️ 追加部分改了 `util.balance.BalanceTable`（A 侧），**需 A 追认**——见《整改方案》§11 越界登记 |
| **3-2** | Boss 震地全局眩晕 | B | **B**（`BossEnemy` 计时）+ **A**（`GameController` 轮询结算） | `BossEnemy` 补"狂暴后每 15s"计时；`GameController.update()` 轮询 `consumeStompRequest()` → 全 `tower.stun(3000)` | 无 | ✅ **已完成（2026-09-11）**：A 侧轮询 2026-09-10 已落；B 侧周期计时本批落地（默认 15000ms，`balance.json` 的 `bossStompIntervalMs` 可调）。`GameController` **未改**（其注释"仅半血狂暴触发一次"已过期，待 A 顺手清理） |
| **3-3** | 狂暴攻击间隔 −30% | B | **B**（`BossEnemy.triggerEnrage`） | `maxAttackCooldown = (int)(maxAttackCooldown * 0.7)` | 无 | ✅ **已完成（2026-09-11）**：改为 `maxAttackCooldown ×(1 − bossEnrageAttackCooldownCut)`，比例入 `balance.json`（默认 0.3 → 1100ms 变 770ms），比原定写死 0.7 更可调 |
| **3-4** | ~~Boss 召唤 2 只~~ | B | — | **已裁定不做**（Q2 关闭，见 §6-①） | — | ⏸ 不做 |

### 批次 4 · 兵营产 2/3 级兵

| 编号 | 任务 | 主责 | 落文件方 | 交付物 | 依赖 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **4** | 兵营产出分级 | C | **C** | `Barrack` 抽 `protected Ally createSoldier()`；`EliteBarrack` 覆写产 `EliteSoldier`、`MasterBarrack` 产 `RoyalSoldier`（全参构造签名与 `Soldier` 一致，**无需改实体类**） | 数值口径**以测试方给出的数值文档为准，本轮不改数值**（见 §6-②） | ✅ **已完成（2026-09-11，C-3）**：`Barrack.java:116-118` 钩子；`EliteBarrack:43` / `MasterBarrack:47` 覆写；**数值一个未动** |

### 批次 5 · 敌人战斗数值可配（**最初点名的核心**）

| 编号 | 任务 | 主责 | 落文件方 | 交付物 | 依赖 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **5-1** | 攻击力 / 攻击冷却入 JSON | F | **F**（`balance.json` 取值 + `BalanceEditorTool`）+ **A**（`BalanceTable` / `GameConfig` / `spawnEnemy` 通道）+ **B**（4 个敌人子类构造扩参） | `balance.json` 增 8 键：`{normal\|fast\|tank\|boss}AttackDamage` + `…AttackCooldownMs`（默认 5/1000、4/700、9/1400、22/1100）；`BalanceTable` 增字段 / `FIXED_KEYS` / `defaults` / `toJson` / `fromJson`；`GameConfig` 增 getter；`spawnEnemy` 传参；4 个敌人构造扩参并**保留旧签名重载** | 无 | ✅ **已闭环（2026-09-11）**：8 键已写入 `balance.json`、B-4 实体扩参、**A 的 `spawnEnemy` 已改用 7 参构造显式传参**（经 `GameConfig` getter）；⏳ 仅 F-1 的编辑器输入框待补 |
| **5-2** | 重甲减伤 + 炮塔破甲 | F | **F**（数值）+ **A**（`tankPhysicalReduction` 通道）+ **B**（`Enemy` 受伤倍率、`Arrow`/`Bomb.onHit`）+ **C**（炮塔破甲参数） | 增 `tankPhysicalReduction`(0.3)；`Arrow.onHit` 走减伤、`Bomb.onHit` 无视（破甲）；飘字改**实际伤害** | 无 | ✅ **已完成（2026-09-11）**：A 通道 2026-09-10 已落；B-5 本批落地（`LivingEntity.damageTakenMultiplier` + `takeDamage` 两参重载 + `TankEnemy` 取值 + `Arrow`/`Bomb` 分流 + 飘字改实际伤害），`tankPhysicalReduction` 已入 `balance.json`。⚠️ **`LivingEntity` 属 A 侧，本批越界改动，需 A 追认**；⚠️ **C 的"炮塔 +30% 加成"未做**（本任务口径只要求"无视减伤"） |

### 批次 6 · 塔 / 士兵数值 JSON 化（二期）　✅ **已落地（2026-09-11 二次裁定）**

> ✅ 测试方与塔负责人再沟通后**推翻同日"直接写死"裁定**：塔数值改走 **`config/tower.json`**
> （`util.balance.TowerBalance` 构造期读取），随 `feature/balancce-json` 合入 `dev`。本批任务
> （下方 `6`、`A-9`、`C-4`、`F-3`）**状态改为已落地/已启用**（编辑器输入框 F-3 仍需补）。
> **炮塔溅射半径 3 键**（3-1）仍走 `balance.json`，保留现状、不回退。

| 编号 | 任务 | 主责 | 落文件方 | 交付物 | 依赖 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **6** | 塔 / 士兵数值入 JSON | F | **F**（数值）+ **A**（通道 + `TowerSpec`）+ **C**（塔类取值）+ **B**（士兵） | 伤害 / 射程 / 冷却 / 溅射 / 士兵参数从 JSON 取 | 需先改《数值配置JSON化规范》§3.4 与 §九红线 | ✅ **已落地（2026-09-11 二次裁定）**：新增 `config/tower.json` + `TowerBalance`，9 个塔类构造期改读 JSON（`feature/balancce-json`，合入 `dev`）。**造价/升级链**未纳入；**溅射半径**按 P1-1 走 `balance.json` |

### 批次 7 · 运行期缺陷（**2026-09-11 新发现**）

| 编号 | 任务 | 主责 | 落文件方 | 交付物 | 依赖 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **7-1** | 士兵锁定目标后不进入待命（`idle` 不可达） | B | **B**（`Soldier`，**不碰抽象基类 `Ally`**） | 目标出视野（`dist > VISION_RANGE=90`）即 `target = null` → `returnHome()` 回岗待命；顺带修 `UnitAnimator.hasOverlay()` 的 `moved` 写死 `true` | 无 | ⬜ **待做**（对应《整改方案》§7 P1-8） |
| **7-2** | 兵营升级 / 出售不清理产出士兵 | C（出出口）+ A（接线） | **C**（`Barrack`）+ **A**（`GameController`） | `Barrack` 交出所产士兵（`getSoldiers()` / `releaseSoldiers()`）；`GameController.upgradeTower` / `sellTower` 调用并把旧兵移出 `allies` | 无 | ✅ **已闭环（2026-09-11）**：C 侧 `Barrack.getSoldiers()`/`releaseSoldiers()` 已就位；**A 侧已接线**（`GameController.retireBarrackSoldiers()`，升级替换前 / 出售后各调一次）→ 新旧两批不再并存（对应《整改方案》§7 P1-9） |

---

## 3. 按角色任务清单（可直接分发）

### A（朱道玺 · 集成/架构）

| 编号 | 任务 | 来源 | 状态 |
| :--- | :--- | :--- | :--- |
| A-1 | `BalanceTable.fromJson` 未知键告警 | 《整改方案》P0-2 | ✅ |
| A-2 | `Tower` 死代码删除 + `level` 改类身份的文档联动（防御塔 / 接口契约 / 数值规范 / 整改方案） | P1-6 | ✅ |
| A-3 | `Main` 登记 `EliteSoldier` / `RoyalSoldier` 动画（落文件方） | 1-3 | ✅ |
| A-4 | 清理 `占位.txt` ×2 + `nul` 垃圾文件 | 1-4 | ✅ |
| A-5 | `Main` 登记 3 个 `*_MASTER` 工厂（落文件方） | 2-1 | ✅ **已闭环（2026-09-11）**：工厂已登记 + E-2 尺寸已补（48×48），L2→L3 全通 |
| A-6 | `GameController` 轮询 `consumeStompRequest()` → 全塔 `stun(3000)` | 3-2 | ✅ **已闭环（2026-09-11）**：B-2 周期计时已落，现为周期性触发（其 javadoc"仅半血狂暴触发一次"已过期，待 A 顺手清理） |
| A-7 | `BalanceTable` + `GameConfig` + `spawnEnemy` 传参通道（8 键） | 5-1 | ✅ **已闭环（2026-09-11）**：通道已落、B-4 实体扩参、**`spawnEnemy` 已改用 7 参构造显式传参**（注入 8 个 `GameConfig` getter） |
| A-8 | `tankPhysicalReduction` 的 `BalanceTable` / `GameConfig` 通道 | 5-2 | ✅ 通道 2026-09-10 已落；B-5 消费 2026-09-11 已落。⚠️ 实际消费走 `BalanceTable.runtime()`，`GameConfig.getTankPhysicalReduction()` 仍无调用方 |
| A-9 | 塔/士兵数值通道（`BalanceTable`/`GameConfig`/`TowerSpec` 取值） | 6 | ✅ **已启用（2026-09-11 二次裁定）**：实际由 `util.balance.TowerBalance` 承载（读 `config/tower.json`），塔类构造期取值；不经 `GameConfig`（同 P1-1 偏离口径） |
| A-10 | 文档汇总修订（接口契约 / 整改方案 / 数值规范 / 分工方案） | 《整改方案》§6 | ✅ |
| A-11 | P1-4 选中通知补线（若决定补 a 方案） | P1-4 | ⏸ 已"放一放" |
| A-12 | `GameController.upgradeTower` / `sellTower` 调用 `Barrack.releaseSoldiers()` 清旧兵 | 7-2 | ✅ **已闭环（2026-09-11）**：新增 `retireBarrackSoldiers(Tower)`，升级替换前 / 出售后各调用一次并 `allies.removeAll(...)` |

### B（邝展颖 · 实体）

| 编号 | 任务 | 来源 | 状态 |
| :--- | :--- | :--- | :--- |
| B-1 | `Bomb` 构造扩参 `splashRadius`，删硬编码 60 | 3-1 | ✅ **已完成（2026-09-11）** |
| B-2 | `BossEnemy` 震地 15s 计时 + 狂暴攻击间隔 −30% | 3-2 / 3-3 | ✅ **已完成（2026-09-11）**：震地改为狂暴后每 `bossStompIntervalMs`（默认 15000）周期触发；攻击冷却按 `bossEnrageAttackCooldownCut`（默认 0.3）削减。两参数均入 `balance.json` 供调试。⚠️ 同 P1-1 偏离：经 `BalanceTable.runtime()` 而非 `GameConfig` |
| B-3 | ~~`BossEnemy` 召唤请求标记（每 20s）~~ | 3-4 | ⏸ **已裁定不做**（Q2 关闭） |
| B-4 | `Enemy` / `FastEnemy` / `TankEnemy` / `BossEnemy` 构造扩参（攻击力 + 冷却），保留旧签名重载 | 5-1 | ✅ **已完成（2026-09-11）**：4 类均扩参为 7 参并保留 5 参重载；**实体类写死值已全删**（含 `Enemy` 基类 5/1000）；旧重载改为从 `balance.json` 取值。⚠️ 消费路径经 `BalanceTable.runtime()` 而非 `GameConfig`（同 B-1 偏离，需 A 追认）；`spawnEnemy` 未改（归 A，5-1） |
| B-5 | `Enemy` 受伤倍率 + `Arrow.onHit` 走减伤 + `Bomb.onHit` 无视减伤 + 飘字改实际伤害 | 5-2 | ✅ **已完成（2026-09-11）**：倍率落在 `LivingEntity.damageTakenMultiplier`（1.0 默认），`takeDamage(dmg, ignoreReduction)` 返回实际伤害；`TankEnemy` 构造期按 `tankPhysicalReduction` 设 0.7；`Arrow` 走减伤、`Bomb` 走破甲；两者飘字均显示实际扣血。⚠️ 越界改了 `LivingEntity`（抽象基类），需 A 追认 |
| B-6 | `Soldier` 目标出视野即释放（修"`idle` 不可达"）+ `UnitAnimator.hasOverlay()` 的 `moved` 写死 `true` | 7-1 | ⬜ **待做**（对应《整改方案》§7 P1-8） |

### C（唐新雄 · 防御塔）

| 编号 | 任务 | 来源 | 状态 |
| :--- | :--- | :--- | :--- |
| C-1 | 3 个精英塔 `render` 改用新 `AssetKey` | 1-1 | ✅ **已完成（2026-09-11）**：实际覆盖 **6 个**精英/大师塔（`ELITE_*` / `MASTER_*` 专属 key），`907bd45` |
| C-2 | `CannonTower.attack` 传 `splashRadius` 给 `Bomb` | 3-1 | ✅ **已完成（2026-09-11，与 B-1 同批）** |
| C-3 | `Barrack` 抽 `createSoldier()` 钩子 + `EliteBarrack` / `MasterBarrack` 覆写 | 4 | ✅ **已完成（2026-09-11）**：`Barrack.java:116` 钩子 + `EliteBarrack:43` / `MasterBarrack:47` 覆写；**数值未动** |
| C-4 | 塔类数值改从 `TowerSpec` / JSON 取（去掉类内常量） | 6 | ✅ **已落地（2026-09-11 二次裁定）**：9 个塔类构造期改为 `TowerBalance.get*` 读 `config/tower.json`；造价/升级链常量保留 |
| C-5 | `Barrack` 交出所产士兵的出口（`getSoldiers()` / `releaseSoldiers()`） | 7-2 | ✅ **已完成（2026-09-11）**：`Barrack.java:124-146`；⚠️ 待 A 在升级/出售处接线 |

### D（张峪嘉 · 交互）

| 编号 | 任务 | 来源 | 状态 |
| :--- | :--- | :--- | :--- |
| D-1 | `GameView.showBossWarning()` 实现（横幅 + 闪屏/震屏） | 2-2 | ✅ **已完成（2026-09-11）**：`GameView.java:786` + 时间驱动绘制 |
| D-2 | 提出 Master 工厂登记需求并确认 `Main` 落文件 | 2-1 | ✅ 已闭环（A 已登记，2026-09-11） |
| D-3 | 确认 1-1「三塔 `render` 改 key」归 C、1-3「`Main` 登记」归 A | 1-1 / 1-3 | ✅ 已确认（1-1 归 C、1-3 归 A） |
| D-4 | 文档修订：《单位行为动画-渲染层设计》《关卡波次配置规范》 | 《整改方案》§6 | ✅ 已改 |

### E（李贤宇 · 素材与音效）

| 编号 | 任务 | 来源 | 状态 |
| :--- | :--- | :--- | :--- |
| E-1 | `AssetKey` 增 3 个精英塔 key | 1-1 | ✅ **已完成（2026-09-11，`e326487`）**：实补 **6 个**（3 `ELITE_*` + 3 `MASTER_*`）+ 对应贴图 |
| E-2 | `assets/sizes.json` 增 3 个 Master 塔条目 | 1-2 | ✅ **已完成（2026-09-11，`sizes.json:15-17` 三条 48×48）** |
| E-3 | 确认精英兵动画帧图可用（供 1-3 登记） | 1-3 | 🟡 四种敌人行为动画帧已入库（含 boss 37 帧，2026-09-11）；精英兵帧图待 E 正式确认 |
| E-4 | 文档修订：《视觉尺寸配置规范》《音效扩展方法说明》 | 《整改方案》§6 | ✅ 已改 |

### F（张少睿 · 测试 + 数值）

| 编号 | 任务 | 来源 | 状态 |
| :--- | :--- | :--- | :--- |
| F-1 | `balance.json` 增 8 个敌人攻击力 / 冷却键的**取值**与 `BalanceEditorTool` 对应输入框 | 5-1 | 🟡 **JSON 取值已落（2026-09-11，B-4 同批写入 `balance.json` 且实战生效）；⏳ 仅剩 `BalanceEditorTool` 输入框 + `FIXED_KEYS`** |
| F-2 | `tankPhysicalReduction` 等减伤 / 破甲数值取值 | 5-2 | ✅ **已完成（2026-09-11）**：`tankPhysicalReduction: 0.3` 已写入 `balance.json`（实现落地后补写，满足"防假通道"前置）。⏳ 仍待 F：`BalanceEditorTool` 输入框补该键 |
| F-3 | 塔 / 士兵数值取值与编辑器页面 | 6 | 🟡 **JSON 取值已落（2026-09-11 二次裁定，`config/tower.json` 已生效）；⏳ 仅剩编辑器输入框/`FIXED_KEYS` 待补** |
| F-4 | **全部整改项的验收**（《整改方案》§10） | §10 | ⬜ 持续 |
| F-5 | 文档修订《建筑与怪物机制策划 v1.1》 | 《整改方案》§6.1 | ✅ 已改 |
| F-6 | 维护 `maps/<key>/waves.json` 波次数据（**测试用例数据，不写死波数**） | §6-④ | ⬜ 按需 |

---

## 4. 文档修订归属（避免多人并行改文档）

| 文档 | 修订方 |
| :--- | :--- |
| 《建筑与怪物机制策划 v1.1》 | F |
| 《数值配置JSON化规范》 | A + F |
| 《关卡波次配置规范》 | D |
| 《防御塔子系统说明》 | C |
| 《友方士兵子系统说明》 | B |
| 《视觉尺寸配置规范》 | E |
| 《单位行为动画-渲染层设计》 | D |
| 《音效扩展方法说明》 | E |
| 《接口契约与抽象类说明》 | A |
| 《整改方案-文档与代码一致性》§3 总表 | A |

> 现状：上述文档的**过时内容修订已完成**（见《整改方案》§6 状态横幅）；本条用于后续新增/再修订时的归口。

---

## 5. 关键路径与批次顺序

```
A: BalanceTable / GameConfig 通道 ──┬─▶ F: balance.json 8 键 + 编辑器 ──┐
                                   │                                   ├─▶ F 验收
B: 实体构造扩参（等 A 通道）       ─┘                                   │
E: sizes.json ──▶ A: Master 工厂登记（L2→L3 打通）─────────────────────┘
```

| 批次 | 内容 | 特征 |
| :--- | :--- | :--- |
| **批次一** | 1-1 ~ 1-4、2-1、2-2、3-1 | 无相互依赖，可**全员并行**；2-1 依赖 1-2 |
| **批次二** | 3-2、3-3、4 | 3-4 已关闭（Q2 不做）；4 的**数值不动**，仅换产出类 |
| **批次三** | 5-1、5-2、6 | **唯一卡点 = A 的 `BalanceTable` / `GameConfig` 通道扩展**；6 为二期 |

---

## 6. 已裁定项（原待定，2026-09-10 拍板）

| # | 事项 | 裁定 | 状态 |
| :--- | :--- | :--- | :--- |
| ① | **Q2：Boss 召唤是否保留** | **不做** → 批次 3-4 关闭，B-3 任务同步取消 | ✅ 已裁定 |
| ② | **批次 4 士兵数值口径** | **数值一律不动**：口径以**测试方给出的数值文档**为准。批次 4 只改"兵营产出哪个兵类"（`createSoldier()` 钩子 + 覆写），**不改任何士兵数值** | ✅ 已裁定 |
| ③ | **P1-4 选中通知** | 维持"**放一放**"（现状 `GameView` 直连 `TowerDetailPanel`，功能可用） | ⏸ 暂缓 |
| ④ | **§8-1 波次数据** | 确认：**测试用例数据、不设固定波数**，由 F 按游戏性需要维护 | ✅ 已确认 |
| ⑤ | **`nul` 垃圾文件** | **已删除**（随 1-4 一并清理，另删 2 个 0 字节 `占位.txt`，共 3 个文件） | ✅ 已完成 |

> 遗留提示：`src/test/java/` 因占位文件删除已变**空目录**，git 不再跟踪；如需保留该目录，请补 `.gitkeep`。

> **第二批交接提示（2026-09-10，A 侧交付后）**：
> 1. **F-1**：`balance.json` 仍为 15 键；`BalanceTable.defaults()` 已含新增 9 键默认值（5/1000、4/700、9/1400、22/1100、0.3），
>    故**编辑器下次保存会把 9 键以默认值写进 JSON**——请一并核对取值；`BalanceEditorTool` 的本地 `FIXED_KEYS` 与 9 个输入框也待补。
> 2. **B-4**：4 个敌人构造扩参（保留旧签名重载）落地后，**通知 A 补 `spawnEnemy` 传参 3 处**，5-1 才算贯通。
> 3. **B-2**：Boss 震地"每 15s"计时落地后，A 侧 `settleBossStompRequests()` 自动变周期性，无需再改代码。
> 4. **E-2**：`sizes.json` 补 3 条 Master 塔（建议 48×48）后，2-1 的"尺寸 48"验收点方可勾选。
> 5. **B-5 / C**：`tankPhysicalReduction` 的消费实现（`Enemy` 受伤倍率、`Arrow`/`Bomb.onHit`、炮塔破甲）与 A 通道并行即可。

---

## 7. 验收与验收人

| 项 | 说明 |
| :--- | :--- |
| 验收人 | **F**（统一验收，《整改方案》§10 为准） |
| 每批编译验证 | A 执行（本机 `javac 17` + `~/.m2/repository/org/openjfx` 的 19 个 jar）：<br>`javac -encoding UTF-8 -d <out> -cp "<javafx jars>" @<sources.txt>`<br>期望：82 源文件 → 114 class、**0 error**（仅既有 unchecked 提示）。<br>✅ **2026-09-10 第二批实测**：82 源文件、**0 error**（仅 2 条既有 unchecked 注记） |
| 画面验证 | D 执行 `mvn javafx:run`（本环境不支持 IDE 直跑，需改 `pom.xml` 的 `mainClass`） |
| 逐项验收要点 | 见《整改方案》§10；其中 5-1 需验证"改 JSON → 重启 → 伤害/频率变化"与"删键 → 回退默认 + 告警" |

---

## 8. 范围红线（不做清单）

- 不新增第二套数值 / 尺寸 / 波次通道；《整改方案》§3 的"目标位置"即唯一归口；
- 不改 `maps/**` 现有路径与塔位语义；波次数据为**测试用例**，不写死波数；
- 不引入第三方 JSON 库（继续用 `util/json/MiniJson`）；
- 不改 `LivingEntity` 公共契约（`MELEE_RANGE` 一致、死亡不碰列表金币）；
- 塔的"换类升级"架构（`ITowerUpgrade` + 原位替换）不推翻；
- 三档士兵"互不继承"的既有取舍不推翻；
- `Main` / `GameController` / `GameConfig` / 抽象基类 / 接口**仅 A 可改**；执行方交付 diff 由 A review 合入。

---

## 9. 附：本方案与《整改方案》的对应关系

| 本方案批次 | 《整改方案》条目 |
| :--- | :--- |
| 1-1 / 1-2 / 1-3 / 1-4 | §8-2 / §8-3 / §8-4 / §8-5 |
| 2-1 / 2-2 | §7 P1-2 / §7 P1-7 |
| 3-1 / 3-2 / 3-3 / 3-4 | §7 P1-1 / §7 P1-3（震地）/ §7 P1-3（狂暴）/ §7 P1-3（召唤） |
| 4 | §7 P1-5 |
| 5-1 / 5-2 | §7 P0-1 / §7 P0-3 |
| 6 | ✅ §7 P0-4（**2026-09-11 二次裁定启用**：`config/tower.json`） |
| 7-1 / 7-2 | §7 P1-8 / §7 P1-9（2026-09-11 新发现） |
| — | §7 P1-4（暂缓）、§8-1（非缺陷，转 F 维护） |
